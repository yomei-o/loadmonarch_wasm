# RESUME - Lord Monarch (DS7E_WIN) を C に戻して native -> WASM

## 対象

Nihon Falcom "Lord Monarch Online" (DS7E_WIN.EXE, 249,856 bytes)。
ユーザー配布の ds7e.zip を orig/ に展開して使う（**リポジトリにはコミットしない**）。

## バイナリの素性（実測）

| | |
|---|---|
| 形式 | PE32 / x86 32bit フラット、6セクション、ImageBase 0x400000 |
| コード | .text 185KB、64,044 命令、**関数 792**（Ghidra が全部復元、失敗0） |
| 言語 | **純 C**（C++ マングル名ゼロ）、MSVC 4.x / linker 3.0 |
| 依存 | KERNEL32/USER32/GDI32/comdlg32/ADVAPI32/WINMM/COMCTL32 のみ。**DirectX 無し** |
| 描画 | CreateDIBSection + StretchBlt + パレット（simtower のシムと同じ形） |
| 音 | MCI (mciSendCommand) で MIDI、timeSetEvent |

## データ形式（全部解読済み）

| | |
|---|---|
| `.256` | uint32 幅 + uint32 高さ + 8bit インデックス。**LOGO をPNG復元して確認済み** |
| `.PAL` | 256 × RGBQUAD (B,G,R,0) = Windows の DIB カラーテーブル |
| `.MAP` | 48×48 の地形バイト + 末尾。**末尾のバイト(offset 0x900)は背景タイルセット番号**（10/20/30/40/50 -> B_010/B_020/…）。難易度ではない。先頭にヘッダは無い |
| `NAME.TXT` | INI（`MAP=15`、`[0]`..`[14]` に NAME=/FILE=） |
| `SOUND.CFG` | テキスト（LOOP=/CD=/WAVE=） |
| `.MID` | 標準 MIDI |
| `.BZ` | Falcom 自前 LZ。**src/bz.c に再実装済み**（下記） |

## .BZ コーデック（0041fe40 / 0041fef0 / 00420240）

コンテナは行単位: uint16 の行長 -> 次の1バイトが 0 ならビット詰め版(00420240)、
それ以外ならバイトオペコード版(0041fef0)。行末に区切り1バイト。
BG/CHR は 1ファイル=1行で、展開後はちょうど **0x8030 = 32,816 バイト**
（ゲーム側が確保するバッファと一致 = 検証になる）。

* バイト版オペコード: bit7 で LZ マッチ（距離13bit/長さ4..7、続く 0x60..0x7f で
  同じマッチを延長）、bit6 で RLE（長さ4bit or 12bit）、bit5 で長リテラル(12bit)、
  それ以外で短リテラル(5bit)。
* ビット版: 0ビット=リテラル1バイト。1ビットの後、続く1ビットが0なら距離1バイト、
  1なら距離 = 5bit(MSB先頭) << 8 | 1バイト。距離0で行終端、距離1は長RLEのエスケープ。
  長さは 2/3/4/5/(3bit+6)/(1バイト+14) のユナリ。
* **`FUN_004204c0` は MSB ファースト**（v = v*2 + bit）。ここを LSB にすると全部壊れる。

## タイル形式（00406810）— 解読済み

展開後のバッファは **4bit プレーナー**（PC-98/EGA 流）。タイル内で
plane0 が +0x00、plane1 +0x20、plane2 +0x40、plane3 +0x60 にあり、同じビット位置の
4bit を集めて 1 ピクセル。最後に全バイトへ **+0x10**（スプライトはパレットの
2 番目の 16 色バンクに載る）。1 バンク = 128 タイル。

* 8x8: 16px 幅プレーン行の左半分（行ストライド 2 バイト）
* 16x16: 素直に行 2 バイト × 16 行
* 32x32: **0x7ca0 の 4 バイト表**が 16x16 サブタイル番号を指す。
  並びは rec[0]=左上, rec[2]=右上, rec[1]=左下, rec[3]=右下（真ん中 2 つが入れ替わる）

実装は `src/gfx.c`。BG の家・城壁・地形、CHR の全方向歩行スプライトが出る。

## .BZ で嵌った 2 箇所（どちらも直した）

* `FUN_004204c0` は **MSB ファースト**（v = v*2 + bit）。LSB にすると全滅。
* ビット版の長 RLE エスケープ（距離==1）の**埋める個数は両分岐とも raw + 0x0e**。
  原典が併せて計算する +0x0f / +0x10 は「消費バイト数」の帳簿用で、個数ではない。
  ここを間違えると CHR は通るのに BG だけノイズになる（16x16 で描くと一目で分かる）。

## 展開先バッファ（原典が確保するサイズ = 検証値）

* BG/CHR のタイルバンク: **0x8030 = 32,816**
* `DATA/data1.bz`: **0x40000 = 262,144**
* `DATA/data1.rgb`: 先頭 **0xc0 = 192 バイト**だけ読んで `FUN_0040e560(dst, buf, 0x80, 0x30)`
  でパレットへ流す（ファイルは 896 バイトあるが全部は使っていない）

`tests/bz_test.exe` で全 40 ファイルが問題ゼロ。

## マップ (00405de0) — 解読済み

`.MAP` は 0x902 = 2306 バイトを丸ごと読む。

* セル 2304 個 (48×48) がそれぞれ **24 バイト (0x18) のランタイム構造体**になる:
  +0x00 = 0x40、+0x0c = dword 100、**+0x10 = ファイルの地形バイト**。
  配列は DAT_0043453c、全体 0xd800 バイト。
* **offset 0x900 のバイトが背景セット番号**で、そのまま 00405fc0（= B_%03ds/m/l.bz
  ローダー）に渡る。値 10/20/30/40/50 が B_010/B_020/… に対応する。
* **地形バイトはタイル番号そのまま**（バンクは 128 タイル）。これで全 15 面が
  草原・森・湖・崖・城・橋つきの正しい地図として描ける（`tests/map_test.exe`）。

## 手順（この順で）

1. ~~**BZ 展開とタイル展開**~~ 済
2. ~~**パレットとマップ描画**~~ 済 — 全 15 面が絵として出る
3. ~~**ネイティブ版の骨格**~~ 済 — `src/win32_main.c`。640x480 のウィンドウ、
   8bit の CreateDIBSection、BitBlt。矢印でスクロール、1/2/3 でズーム(8/16/32px)、
   PgUp/PgDn でステージ切替。**mingw と MSVC 両方でビルド確認**
   （mingw: `gcc -mwindows ... -lgdi32`、MSVC: `tools/build_msvc.bat`）
4. **セルの掃引**（00417380）、**ユニットの腕**（0041d870）、**プレイヤーの注文**
   （0040b330 / 0041a8d0 / 0041e670 / 00420b30）、**エンティティ巡回と移動**
   （004204f0 / 00401000 の歩行）を移植 — `src/sim.c`。
   次の候補: 一般ユニットの振る舞い 00401770、目的地の衝突解決
   （00420c60 / 00420610 / 004208b0）、0041b370 前半の戦力集計、
   そしてリーダービットを立てている本来の場所
5. **WASM 化**。描画が DIB+StretchBlt なので simtower_wasm のシムがほぼ流用できる。
   `src/render.c` は 8bit インデックス面に描くだけなので、そのまま canvas に渡せる

## 地形バイトは「タイル番号」と「セルの状態」を兼ねている

これが一番大きな発見。毎フレームの掃引 00417380 は 2304 セルを巡回カーソルで
**1回に 0x8f = 143 セル**処理し、セルの地形バイトで分岐する:

| 地形値 | 意味 | 原典 |
|---|---|---|
| 5 | 中立の湧き点 | 0041dec0 |
| 8 + f | 勢力 f のユニットがいる | 0041d870（未移植、1005バイト） |
| 0x0c + f | 勢力 f の占領地 | switch 内でインライン処理 |
| 0x14 + f | 勢力 f の**城** | 0041dc60 |

それ以外（普通の地形）は掃引が触らないので変化しない。**全15マップに
0x14/0x15/0x16/0x17 が各1個ずつ**あり、これが4勢力の初期城。0x08〜0x0b と
0x0c〜0x0f も初期配置としてマップに入っている（B_000 なら領土 242/67/80/161）。
0x7a は特別なセル（0040b680 が価値 +0xff する）。

### 中立の湧き (0041dec0)

価値が 0xf5 未満なら +10 して終わり。超えたら 0xf5 を消費し、そのセルに誰も
いなければ空きエンティティを1つ確保して置く（勢力 4、+0x08=200、+0x0c=6、
+0x0d=0x0e、+0x0f=10、+0x18=0x1f0）。

### ユニットの腕 (0041d870) — 領土拡大エンジン

これがロードモナークの中核。地形 `8 + f` のセルごとに:

* **勢力フラグ bit6 (0x40) が立っているとき**は塗り替えモード。`at1f` が 4 なら
  そのセルと 3x3 の自勢力地を 0 に消す。4 以外なら `at1f + 8` / `at1f + 0x0c` に
  書き換える（勢力の乗っ取り・消滅の表現）。
* **bit0 が立っていれば何もしない。**
* 通常は成長: 8近傍を数え（0041ee10）、
  `セル価値 += 自勢力地の隣接数 + 1`。
  `(隣接数 + 1) * 0x10 <= 価値` になったら価値を 0xff で頭打ちにして、
  * 空き地の隣接があればその**最初の1つ**を `f + 0x0c`（自勢力地）にする
  * 空きが無ければ 0041c780 が**他勢力の地**（0x0c..0x0f で自分以外）を1つ選んで奪う
  * どちらかを取れて価値が 200 未満ならそこで終わり
  * **どちらも取れない（内陸で囲まれている）場合は価値に関係なく次へ進む**
    — 原典もこの構造で、囲まれたセルは即座にユニットを生む
  * 価値 200 以上（または上記の抜け道）で、セルに自勢力のエンティティが
    立っていればその +0x08 に価値を足して価値を 1 に戻す（10万で飽和）。
    いなければ空きエンティティを確保して `+0x08 = 価値 - 1` で作る。
    人間の勢力かつ DAT_004365e0 != 1 なら `flags |= 4` と
    `+0x0d = order | 0x10`（インターフェイスで選んだ命令）、それ以外は `+0x0d = 1`。

8近傍のオフセットは 00434420 / 00434428 の対で
dx = {-1,1,0,0,-1,1,1,-1}、dy = {0,0,-1,1,-1,-1,1,1}。

移植の確認: B_000 は初期ユニット (0x08..0x0b) を持たないので何も起きない。
初期ユニットのある **B_003** で走らせると領土が 32/32/54 -> 88/88/89 に伸び、
エンティティが 64 個上限まで埋まる。

### エンティティ側の巡回 (004204f0)

セルの掃引とは別に、**エンティティ 64 体を巡回するカーソル**がある。1回に 0x3f 体
（1回目は 1..63、次の回で 0 に折り返す — スロット0を1回目に踏まないのが罠）。
役割で5つに分岐する:

| 条件 | 原典 | サイズ |
|---|---|---|
| flags bit1（瀕死） | 00420af0 | 62B |
| 勢力 == 4（中立） | 00402700 | 1216B |
| +0x0d bit5（リーダー） | 00401000 | 1429B |
| +0x0d bit4 かつ flags bit2 | 00402bc0 | 1296B |
| +0x0d bit4 | 00403170 | 4350B |
| それ以外（一般ユニット） | 00401770 | 3931B |

移植済みは瀕死（3ティックで退役）とリーダーの歩行。残りは合計 1 万バイト超なので
手を付けず、推測で書いていない。

### 一般ユニットの自律行動 (00401770) と維持費 (0041a920)

**`+0x0d` の下位4ビットが命令コード**で、一般ユニットはそれを自分で実行する。
これが「国が勝手に育つ」正体。

* **命令 1 / 2 / 3 は `0040b330`（ユニット設置）** — 経路が無いとき、その場で実行する。
  つまりプレイヤーの注文と同じ処理をユニットが自分で呼ぶ。
* **命令 0x0c は「リーダーを失った」状態**。勢力の `+0x1f` が指す勢力に寝返る
  （4 なら死ぬ）。寝返り先が人間の勢力なら命令 0 になる。
* 他勢力の**城の上に立つと**経路を捨て、向きを 6 にして命令 2 になる。

**維持費 (0041a920)** — 行動の前に必ず通るゲート:

* 城の上、または**自勢力のユニットセル**の上にいるなら、国庫から `戦力 >> 11` を払う
* そうでなければ**自分の戦力を `(戦力 >> 8) + 1` だけ食う**。尽きると死亡マーク
  （00420aa0 が flags bit1 と原因コードを立て、巡回の瀕死アームが3ティックで退役）

**通過による領土消去 (00420a40)** — 他勢力の領土（0x0c..0x0f、自分と `+0x1e` の
同盟を除く）の上を歩くと、そのセルを 0 に戻す。

未移植: 目標へ向かう経路探索（0041ef80 経由）と命令 4 以降。移植では
その場に留まるだけで、勝手な動きを作っていない。

### 移動の仕組み (00401000 / 0041d690 / 0041d6d0 / 0041ec30)

* **エンティティの 0x224 バイトの大半は経路バッファ**。+0x1c から 1 バイト 1 方向で
  最大 0x200 歩、+0x18 がその添字。**+0x18 == 0x1f0 が「経路なし」**の印で、
  そのときの方向は既定の **5**。
* 方向は 8 方向で、0041d6d0 がスタックに積む 16 ワードから読むと
  dx = {-1,-1,0,1,1,1,0,-1}、dy = {0,-1,-1,-1,0,1,1,1}（0=西、2=北、4=東、6=南）。
* **向きが違えばまず向きを変えて 1 ティック消費**（+0x0c が現在の向き、+0x0e にも複写）。
  向いてから 1 マス進む。だからユニットは歩き出す前に一度その場で向きを変える。
* 進めるのは境界内（0041ec30 は 0 < 座標 < 0x30 を要求）かつ地形 0x30 未満かつ
  誰も立っていないセル。人間の勢力以外は進めないと経路を捨てる（+0x18 = 0x1f0）。
* 目的地に誰かいる場合の解決は 00420c60 / 00420610 / 004208b0 が担当していて、
  そこは未読。移植では**進まずに止まる**ようにしてあり、戦闘を勝手に作っていない。

### 城と経済 (0041dc60)

**勢力 +0x1c は税率**で、自分の資金から決まる（貧しいほど高い）:
* 人間の勢力（DAT_004365cd）は DAT_0043769c==1 のときだけ再計算し、
  `0x14 - (funds >> 8)`（0x15 以上なら 0）
* それ以外は `~((funds >> 8) << 2) & 0x1c`、ただし `funds & 0xffffff00 < 0x701` のときだけ

徴収は、城のセルに自勢力のエンティティが立っていて（そのエンティティの
+0x0d bit5 が立ち）、**2セル南**（原典は cell+0x40 = cell[index+2]）が
0x30 未満かつ自勢力のものであるときだけ走る。全セルを走査し、地形が
`8 + 自勢力`でコスト(+0x08) < 0x1f0 のセルから `税率 * 価値 >> 8` を取り、
そのぶんセルの価値を減らす（資金は 100000 で飽和）。

移植の確認: B_000 を読み込んで動かすと、税率が人間 1 / AI 0（原典の算術どおり）、
領土数がマップの実データ通り、25 周ほどで中立セル 6 個すべてがエンティティを
生成する。

## ゲーム状態の3配列（`src/state.h` に移植済み、`tests/state_test.exe` が検証）

| 原典 | 個数 | ストライド | 総バイト | 何 |
|---|---|---|---|---|
| DAT_0043453c | 2304 | 0x18 | 0xd800 | セル (48x48) |
| DAT_00434538 | **64** | 0x224 | 0x8900 | エンティティ（町/城とみられる） |
| DAT_00434534 | 5 | 0x38 | 0x118 | 勢力（うち 4 つだけが敗北判定される） |

エンティティ: +0x00/+0x01 = 列・行、+0x02/+0x03 = 目標の列・行（1..46 に制限）、
+0x04 = 勢力番号、+0x10 bit7 = 非アクティブ、+0x18 = 0x1f0、+0x21c bit0、+0x220 = 0xff。

勢力: +0x04 bit4 = 敗北、+0x10 = 戦力（+0x28 と +0x30 の和、100000 で飽和）、
+0x18 = 資金（初期 5000）、+0x1f = 4（敗北時）、+0x24 = 保有エンティティ数。

初期化列（00407790 が呼ぶ順）: 004273b0 -> 00405330 -> 004272b0 -> 00427210 -> 0041b370。
`stateStartStage()` がこれ。ただし 004273b0（全セルを地形 0x30・価値 100 で埋める）は
**マップ読み込みより前**に走るものなので、移植では繰り返していない。

## セル構造体 24 バイト（判明分）

| offset | 型 | 用途 |
|---|---|---|
| +0x00 | byte | ロード時 0x40、のちに陣営バイト（`DAT_0043756c` から） |
| +0x04 | byte | 領域を辿る間 1 が立つ（フラッドフィルの訪問済み） |
| +0x08 | uint | `<` で最小を探す = **経路コスト** |
| +0x0c | uint | ロード時 100、のちに `(v >> 1) + 1` |
| +0x10 | byte | **地形タイル番号**（ファイルそのまま） |
| +0x14 | uint | 0 クリア後、現在数から設定 |

**インデックスは `col * 0x30 + row`**（0x30 = 48）。つまり添字の前半が**列**、
後半が**行**。素直に読むと逆に取りたくなるが、原作と並べて確認した結果こちら。
`WORLD_INDEX(col, row)` を使う。ここを間違えるとマップが対角線で転置される
（地形の雰囲気は合っているのに崖や海岸の向きが違う、という見え方になる）。

## 原作は Windows 11 で動く

`orig/DS7E_WIN/DS7E_WIN.EXE` はそのまま起動する（32bit PE、Win95 時代だが動く）。
**移植の正解データとして使える** — `tools/orig_shot.ps1` がウィンドウを撮る。
ただしスクリーンショットの色は当てにならない（8bit パレットアプリなので
デスクトップ合成で色が化ける）。幾何・配置の確認には使える。

## 命令付きユニットと行き先選び (00402bc0 / 0041e0a0 / 0041e560)

ユニットが「どこへ行くか」を決めるのはこの2つ。どちらも 00434400 / 00434410 の
オフセット（1マス隣の4方向 -> 2マス先の4方向）を順に見て、**通れなかった方向を
シフトするマスクに記録し、4歩前に塞がれていた候補は飛ばす** — これが壁の向こう側の
目標に向かって歩き出すのを防いでいる。

* **0041e0a0**: 地形が 8..0x0b（＝他勢力の集落）で、自分と `+0x1e` の同盟でない
  ものへ向かう。命令 4 がこれを使う（＝攻撃）
* **0041e560**: 地形が `自勢力 + 8`（＝自陣の集落）へ向かう。ただし**既に自陣の
  集落に立っているなら 0 を返して留まる**。00403100（既定の行動）がこれを使う

00402bc0 の命令: 0 = 待機、4 = 敵へ、5 = 設置、その他 = 00403100（命令を1に戻して
帰還）。前段に必ず維持費 0041a920 と隣接攻撃 0041ef80 が入る。

通行可否は 0041ebf0（`0 < 座標 < 0x2f` かつ `+0x05` が 0）。

移植の効果: B_004 で 4勢力すべてが資金と税率を動かし始め、B_003 ではエンティティが
64上限に張り付かず 10〜24 で推移する（自陣外で飢えて減り、集落へ帰る）。

## 命令付きの歩行 (00403170)

00401000 の歩行と同形だが、**塞がれると `+0x0f` の我慢カウンタを1減らし、0 に
なったら経路を捨てて命令1（帰還）に戻る**。これがあるのでユニットは壁の前で
永久に立ち止まらない。中立の湧きが `+0x0f = 10` を、種のリーダーが 4 を持つのは
このカウンタ。

原典は諦める条件を 0041eb60 と 0041a800 でさらに絞っているが、どちらも未読なので
移植ではカウンタだけで判断している。

移植の効果: B_004 で勢力3の資金が 6 まで落ちて税率 28（貧しいほど高く取る原典の
式どおり）、B_006 は4勢力すべてが税率 16〜24 で維持費を払い続ける状態になる。

## 中立の徘徊 (00402700)

`+0x0f` が徘徊タイマー。**bit7 が立っていれば休憩中**（0 になったら
`random(0x14)*2 + 0x14` を入れて再開）、0 なら 11分の1 の確率で寝る（`r | 0x80`）、
それ以外は**偶数tickだけ動く**。だから中立の小さい人影はのんびり歩く。

移動は今の向きへ1マス。地形が 0x30 未満で**かつ 0x1d でない**こと（徘徊者が
乗らない唯一の地形）が条件。ぶつかった相手は戦闘 -> 合流 -> 集落略奪の順で処理し、
誰かが立っているだけなら向きを変える（`random(100) < 0x32` で反転か直角）。

方向選択のマスク走査（0041f020 を4方向、だめなら8方向）は未読なので、移植では
新しい向きを乱数で選び直している。乱数も原典の Microsoft C rand() ではなく
同じ形の自前のもの（**原典と同じ数列にはならない**）と明記してある。

移植の効果: B_004 で中立に 18,336、勢力3に 837 の損害が出る — **戦闘が実際に
起きるようになった**。S_115（中立90セル）では60体が徘徊する。

## +0x0d は下位ニブルだけではない (00403170 の case 4)

命令は `+0x0d & 0x0f` だが、**上位ビットも読まれる**:

| ビット | 意味（00403170 case 4 より） |
|---|---|
| 0x80 | 追跡を続ける — 0041e0a0 で敵の集落を探す |
| 0x40 | `+0x0d = 0x10` に戻す（命令なしの待機） |
| 0x20 | リーダー（00420b30、0041dc60 が見る） |
| 0x10 | 命令付き（ディスパッチャがこのビットで腕を選ぶ） |

**0x80 が無いと命令4は帰還になる**。ページの「全軍 攻撃」はこのビットを一緒に立てる。

case 2 は「南を向いて設置」。**case 6〜0xc（数百行）は未読**で、移植では既定の
帰還にフォールバックしている — 何をするか勝手に作らない。

移植の効果: B_004 で4勢力すべてが損害を出し（773/662/994/1158）領土も減り始める。

## 命令のレパートリー（全部そろった）

`+0x0d & 0x0f` が命令で、00403170 はそれぞれ専用のアクション関数を呼ぶ。
どれも共通の尾部（上位ビットで追跡/待機/帰還を選ぶ）を持つ。

| 命令 | 原典 | 何をする | 対象 |
|---|---|---|---|
| 0 | — | 待機（南を向く） | — |
| 1,3,5 | 0040b330 | **集落を建てる**（資金100＋戦力200） | 立っているセル |
| 2 | 0040b330 | 同じ（先に南を向く） | 立っているセル |
| 4 | 0041e0a0 | **敵の集落へ向かう**（上位 bit7 必須） | 近傍 |
| 6 | 0040b440 | **壁を建てる/補強**（地形 0x7b） | 目標 |
| 7 | 0040b680 | **開拓**（0x30..0x5f -> 0x20） | 目標 |
| 8 | 0040bc20 | **建造物を壊す**（地形 1..4、戦力の 1/2） | 立っているセル |
| 9 | 0040b840 | **壁を壊す**（0x7b -> 0、戦力の 1/32） | 目標 |
| 10 | 0040b960 | **鉱脈を造る**（0x20..0x2f -> 0x7a）／既存を 0xff まで養う | 目標 |
| 0xb | 0040bb10 | **中立の湧き点を潰す**（地形 5 -> 0x60、戦力の 1/8） | 目標 |
| 0xc | — | リーダー喪失状態（寝返るか死ぬ） | — |

これで**地形の値の意味がほぼ全部つながった**:

| 地形 | 意味 |
|---|---|
| 0 | 素地 |
| 1..4 | 建造物（命令8で壊せる） |
| 5 | 中立の湧き点 |
| 8+f | 勢力 f のユニット/集落 |
| 0x0c+f | 勢力 f の領土 |
| 0x14+f | 勢力 f の城 |
| 0x20..0x2f | 開拓済みの土地（命令10で鉱脈にできる） |
| 0x30..0x5f | 障害物（命令7で開拓できる） |
| 0x60.. | 素の風景 |
| 0x7a | 鉱脈（安く大量に採れる） |
| 0x7b | 壁（通行不可） |

## 壁 (0040b440) — 命令 6

**地形 0x7b が壁**。素地（0）または自陣（0x0c..0x10）に建てられる:

* 投入 work は `価値 + 0xff` と `戦力 >> 4` の小さい方、**費用は work の 1/4**
  （開拓の 30 に対してずっと安い）
* 価値が残れば減らして進行中、尽きたら**地形を 0x7b にして余りを新しい価値に**
* 既存の壁（0x7b）は **0xff まで補強**できる（`0xff - 価値` の分だけ）
* 目標セルに誰か立っていれば拒否

壁は 0x30 以上なので**通行不可になる** — だから原典は建った瞬間に 00405330 を
呼んで全セルの `+0x05` を再計算する。移植も同じことをしている。

## 開拓 (0040b680) — 命令 7

ユニットが `+0x02/+0x03` の目標セルの障害物を削る:

* 地形 **0x30..0x5f** が障害物で、1単位の work につき **資金30**、work は `価値 + 1`
* 地形 **0x7a** は特別で、**資金2**、work は `価値 + 0xff`（安く大量に進む）
* 投入できる work は `エンティティの戦力 >> 4` と上限 work の小さい方
* 価値が残れば減らして「進行中」（原典の戻り値4）、尽きたら**地形を 0x20 に変えて**
  余り（`amount - 価値`）を新しい価値にする（戻り値1）

0x20 は 0x30 未満なので**歩けるようになる**。これが「土地を開く」正体。

原典は先頭で 0041ac10 を見ているが未読なので、移植では 0040b680 自身の次の
ガード（目標が 1..46 の範囲内）だけを使っている。

## 00403170 のまだ読んでいない場所

`decomp/functions/FUN_00403170.c` の行番号で: case 8 は 293、9 は 344、10 は 396、0xb は 455、0xc は 507。
case 2/4/5/6/7 は移植済み。それぞれ 30〜60 行ある。

## スプライト番号 (0041b520) — 表ではなくビット詰め

これが分かるまでスプライトは正しく出なかった。番号は**計算**される:

```
通常   サイズ級 | 勢力<<3 | 向き | コマ         0x00..0x7f
作業中 0x80/0x82/0x84/0x86 | 勢力<<3 | コマ     0x80..0x9f
戦闘中 0xa0（リーダーは0xa4）| 勢力<<3 | 相     0xa0..0xbf
中立   0xc0 | 向き | コマ / 0xc8 | 相           0xc0..0xcf
カーソル 0xcc, 0xcd（セル+0x14 から）
```

* サイズ級: 戦力 1000 未満 0、10000 未満 0x20、それ以上 0x40、**リーダーは
  戦力に関わらず 0x60** — リーダーは大きさではなく位で描かれる
* 向き = `+0x0c & 6`（4方向）、コマ = `frame & 2` の1ビット、相 = `+0x0e`
* 状態は `+0x10` の bit1（戦闘）と bit0（作業）
* **最大 0xcd = 205。バンクは 208 タイル** — ユニット全部とカーソル2コマで
  ぴったり埋まる。これが番号解釈の正しさの証明になっている

## バンクの確保表 (00407420)

| 用途 | 大きさ | タイル数 |
|---|---|---|
| 地形 8/16/32 | 0x2000 / 0x8000 / 0x20000 | 各 **128** |
| スプライト 8/16 | 0x3400 / 0xd000 | 各 **208** |
| スプライト 32 | 0x40000 | **256**（4ファイル×64） |
| UIシート | 0x40000 | 256×1024 |
| セル | 0xd800 | 2304 = 48×48、1つ 0x18 |
| エンティティ | 0x8900 | 64、1つ 0x224 |

ファイル名は地形 `B%03d{s,m,l}.bz`、スプライト `C%03d{s,m}.bz` と
`C%03dl{1..4}.bz`。

## 展開ルーチンは1つ (00406c70)

タイルサイズを引数に取る単一関数。3分岐すべて 4プレーン（+0x00/0x20/0x40/0x60、
MSB先頭）から4bitを組み、`0xf` は透明、他は **+0x30**。

* **16px**: 0x80バイト = 1タイルを素直なラスタ順で。208回
* **8px**: 0x80バイト = 16×16 を作り、**TL, BL, TR, BR** の順に4枚切り出す。
  52ブロック = 208タイル。**透明ニブルは 0x70 ではなく 0x76**（最後の
  ブロックだけ 0x70）。原作のループは 0x2000 まで回るのでファイル外を12ブロック
  読み、208タイルのバンクに256枚書く — 移植では 52 で止めた
* **32px**: 0x200バイト = 1タイル。4分割のプレーンが 0x00(左上)/0x80(右上)/
  0x100(左下)/0x180(右下)。64タイル×4ファイル

## 脈打つパレット (0040a6f0)

毎ティック、明るさ表 `{100,90,80,70,60,50,40,30,40,50,60,70,80,90,100,100}`
（0x4321d0）の `frame & 0xf` 番目を取り、保存しておいた色に掛けて

* **0x1f** に（地形の 0x1f の元色 × 明るさ）
* **0x76** に（スプライトの 0x3f の元色 × 明るさ）

を書き込む。**8px のユニットは 0x76 を下地に敷いてある**ので、いちばん引いた
表示で駒が点滅して見える。これが「8pxバンクは文字に見える」の答えでもあった —
文字に見えていたのはリーダーの記章。

他に 0x70 に RGB(0,0,0x96)、0x71..0x75 に5勢力の色（スプライト色の複製）、
0x50..0x5f に白16色。**0x71..0x75 の元ネタは未読**（0x435b40 付近の17バイト
レコード表）。

移植では `renderPalette()` が256色表を1か所で組む。3つのホストが自前で
帯を見分けていたのをやめた — 帯は今 4つあり（地形0x10 / スプライト0x30 /
UI 0x80 / 脈動 0x1f と 0x76）、どれか1つ落とすと別の帯の色で描かれる。

## 描画は「セルを掃く」(004240c0)

エンティティ配列ではなく**セルを掃いて**描く。1セルあたり:

1. 地形 `+0x10` を地形バンクから（前フレームと違うときだけ = ダーティ判定）
2. **`+0x00` が 0x40 未満なら、それは「そこに立っているエンティティの番号」** —
   所有者ではない。0041b520 の番号で描く
3. そのエンティティの `+0x220` が 0xff でなければ**命令の吹き出し**を1セル上に
   （`DAT_004376a1` が表示スイッチ）
4. `+0x14` が非0なら、その番号 `+ (frame & 1)` を描く — **カーソル（0xcc）**が
   ここに置かれる。0040b270 が移動時に前のセルを0にして新しいセルに書く

吹き出しは**スプライトバンクではなく UIシート**から: `x = 0xc0 + タイルサイズ`,
`y = 番号 * 32`、大きさ ts×ts。3つのズームが 0xc8 / 0xd0 / 0xe0 に隙間なく
並ぶ（0xc8-0xcf, 0xd0-0xdf, 0xe0-0xff）。透明は 'p' = 0x70。

セル添字も確定した: 転送先が `(a * ts * 0x30 + b) * ts` なので **a が画面の行
ではなく列**… ではなく、a が y、b が x。セル添字は `a + b * 0x30` で、
b が画面上の x。つまり `WORLD_INDEX(col, row) = col * 0x30 + row` で
col が画面の x — 経験的に決めた向きが正しかった。

## メインティック (0040a5e0)

ダイアログが開いていない・一時停止でない・マウスが特定の窓の上にない、を確認して

1. `DAT_00435b1c++`（アニメーション兼パレットのカウンタ、初期値1）
2. 0041d740, 004204f0（セルの掃き）
3. **0040b270**（カーソル移動）
4. **0041b370**（集計と敗北判定）← **毎ティック呼ばれる**
5. 0041b4f0、マップ窓の再描画（vtable+0x64）
6. 0040a110, 0041f790, 0041f090, 0041f4c0
7. **0040a6f0**（パレット脈動）

マップ窓は親窓の**上から 0xb0 = 176 px 下**に置かれる (00422740)。つまり
インターフェイスの帯は上端176px。窓の大きさは `タイルサイズ × 0x30`。

## 名前テーブル (00405fc0 の末尾) — シナリオごとの国名と命令名

**大きい地形ファイル `B_%03dl.bz` の 0x7ea0** に 16バイト×22レコードの表がある。
先頭13バイトが使える（読込側が 13..15 を 0 で埋めている）。

| レコード | 中身 |
|---|---|
| 0 | ゴミ（"Online/..."） |
| **1..5** | **5つの国の名前**。先頭バイトは文字ではなく**その国の色**（地形パレットの添字）で、原作はそこに空白を上書きする |
| **6..21** | **16種の命令名**、命令番号 0..15 の順 |

国の色は `(先頭バイト & 0xf) + 0x30` でスプライトパレットを指し、それが
**パレット 0x71..0x75** に入る（インターフェイスの絵がその色で描かれる）。

シナリオごとに全部違う。実際に入っているもの:

* set 10: Greenland / Redland / Whiteland / Blackland / Monsters、
  命令は Standby, Auto, Defend, Reinforce, Destroy, Build, Barricade,
  Build Bridge, Clear Land, Clear Path, Dismantle, Seal Cave, Fall Cave,
  I'm Leader, Free Leader, Break Leader
* set 20: **Weeburger / Banzai Sushi / Octosnacks / Snack Carts / Mobsters**、
  命令は Take Break, Auto, Ambush, Rally Help, Gone Broke, Now Open,
  Toss Trash, Build Path, Develop, Clean Trash, Block Path, Demonstrate,
  Merger, Number One!, Free, Collect Money
* set 30: 三国志（LiuBei / SunQuan / LuBu / CaoCao / Tornado）
* set 50: Witchland / Indyland / Mushroomland / Honeyland / Bad Boys

**命令番号の対応がこれで確定した**: 番号 N = レコード 6+N。番号 1 が "Auto"、
番号 4 が "Destroy"（=近隣の集落を襲う、`0x80` 付きで実装済み）で、
移植の挙動と名前が一致する。

`worldCountryName()` / `worldOrderName()` で引ける。WASM 側は
`lm_country_name` / `lm_order_name`。

## パレットの帯（現時点で分かっている全部）

| 添字 | 中身 |
|---|---|
| 0x10..0x1f | 地形の16色。**0x1f だけ毎ティック脈動** |
| **0x20..0x2f** | 地形の16色を **0x55/100 = 85%** に落としたもの（00405fc0）。**まだ誰も使っていない** — どこで陰として使われるのか未調査 |
| 0x30..0x3f | スプライトの16色 |
| 0x50..0x5f | 白16色（3035行）。用途未調査 |
| 0x70 | RGB(0,0,0x96)、インターフェイスの下地 |
| 0x71..0x75 | 5つの国の色（名前テーブル由来） |
| **0x76** | スプライト 0x3f の**脈動する複製**。8pxユニットの下地 |
| 0x80..0xaf | data1.rgb の48色、インターフェイス |

`renderPalette()` が1か所で組む。

## 上部インターフェイスの構造（分かったこと・未実装）

画面は上 176px がインターフェイス、その下がマップ (00422740)。
上の帯には **176×176 のパネルが3枚**並ぶ:

| 窓 | 内部コード | 位置 |
|---|---|---|
| Progress Window | Code1 | x=0 |
| Unit Window | Code0 | x=0xb0 = 176 |
| Graph Window | Code3 | x=0x160 = 352 |
| Map Window | — | y=0xb0 = 176 |
| Frame Window | Code7 | 全体 |

**パネルの中身は絵ではなく GDI のテキスト**だった（1880-1940行）。
Graph Window は1勢力あたり2行、8行を組み立てる:

* `"%s Funds: %d"` あるいは `"%s Area Occupied: %3.2f"`（**+0x34 は float**、
  占有率）
* `"%s Leader Strength: %d"`（**+0x0c はリーダーのエンティティ番号**。
  0x40 = リーダー無し。「0x40 が入る」と書いていたのはこれ）
* `"%s Unit Totals: %d"`（+0x10 = 戦力）
* `flags & 0x40` なら `"%s Defeated"` ← **敗北表示はビット6**。
  0041b370 が立てるのは 0x10 なので、**2つは別のビット**。使い分け未確認

`%s` は名前テーブルの国名。**原作が OS のテキスト描画を使っているので、
WASM 版はページ側の HTML で出すのが素直**（そのための材料は全部 C から
公開済み: `lm_country_name`, `lm_funds`, `lm_tax`, `lm_strength`, `lm_out`）。

未読の部品: ダイアログ・一覧・ゲージの絵（シートの 0x2c000 / 0x38030 /
0x3a000 / 0x300c0 / 0x380c0 / 0x30090 / 0x33090 を `+0x244 * 0x100` 付きで
使う 11900-13300 行の一群）、吹き出しの下地 0x20000（208×64）。

## 命令の選択 (0040a020 / 00409f90)

* **0040a020(entity, col, row, force)** — プレイヤーの部隊を選ぶ。自勢力で
  戦闘中(flags&2)でないことが条件、force=0 なら既に命令済み(at0d bit4)を弾く。
  通ると `flags21c |= 1`、**`+0x220 = 0`（吹き出し番号）**、`at0e = at0c`、
  `at0c = 6`。ついでに 00405360 + 0041a680 を呼ぶ
* **00409f90** — 選択の解除/確定。`flags21c bit0` が立っている全部について
  `at0c = at0e` に戻し、ビットを落とし、**`+0x220 = 0xff`**。一時停止も解除

**`+0x220` は 0 か 0xff の2値しかない** — 吹き出しの絵は1種類。

## 到達判定つきの徴税（**実装済み**）

0041dc60 が徴税の前に呼ぶ3つ、全部移植した:

1. **00405360** `simResetFill` — 全セル `marked(+4) = blocked(+5)`,
   `cost(+8) = 0x1f0`（このゲームの無限）
2. **00405390** `simBlockForeign` — 他勢力のユニットセル(8+f)と領地(0xc+f)を
   `marked = 1`。ただし `factions[me].at1e` が指す同盟国は通す（初期値 0x80 = なし）
3. **0041a680** `simFillFrom` — 幅優先探索。近傍 dx={-1,1,0,0} dy={0,0,-1,1}
   （0x434401/0x434411）、通行可否は 0041ebb0 =
   `1<=col<=0x2e && 1<=row<=0x2e && marked==0`、`cost[隣] = cost[今] + 1`
4. `地形 == 8+勢力 かつ cost < 0x1f0` のセルだけが `funds += 税率 * value >> 8`

キューは 256 バイト2本で head/tail が byte。2304セルのマップに対して危なく
見えるが**溢れない** — キューが持つのは訪問済みではなく**フロンティア**で、
48×48 の盤で 255 に届くことはない。**訂正**: 以前ここに「溢れて早く止まる」と
書いたが誤り。開けた盤で届く 2116 セルは、**外周188マス（0041ebb0 が絶対に
歩かない）を除いた全部** = 2304 - 188 で、ちょうど一致する。

### 起点だけは原作が書いていない（**移植側の判断**）

`factions[f].at08[0..1]` が起点。この2バイトは 00423f90（その国の本拠地に画面を
寄せる）と 00421270（退却先）でも読まれるので**首都の座標**で間違いない。
ところが **792関数のどこにもこの2バイトへの書き込みが無い**（`decomp/functions/`
を全部走査して確認）。004272b0 の初期化は 0 埋めするだけ。セーブファイルの
読み込み（0x16218 = 0x118 + 0xd800 + 0x8900）だけが値を入れる。

つまり原作のコードを字義通りに動かすと、**新規開始の面では起点が (0,0) で
0041a680 が即 return し、どのセルも 0x1f0 のままで税収がゼロになる**。
そうはならないはずなので、移植では `fillOrigin()` が
**at08 が 0 なら地形 0x14+f の城を探して起点にする**フォールバックを持つ。
**これは原作にはない、こちらの判断**。at08 を誰が書くのかは未解決の宿題。

### 税率は「貧しいほど高い」

`funds & 0xffffff00 < 0x701` のときだけ `rate = ~((funds>>8)<<2) & 0x1c`。
資金 0 で 28、1792 以上で 0。人間側（自動税率 on）は
`rate = funds>>8`, `rate < 0x15 ? 0x14 - rate : 0`。
**資金 5000 なら税率 1、収入はほぼ 0** — 使って減れば税率が上がる自動調整。
ハーネスで資金が動かないのはこれで正しい。

## tools/lowpri.sh が失敗を握り潰していた（修正済み）

`cmd /c start /WAIT` は**子の終了コードを返さない**（常に 0）。build_tests.sh は
`set -e` なのに素通りし、**コンパイルが失敗しても前のバイナリでテストが走っていた**。
実際これで「テストが通っているのに新しい検査が出力されない」に一度嵌った。

今はコマンドをバッチファイルに書いて終了コードをファイル経由で拾う。嵌りどころ2つ:

* 引数を**ファイルに書くと MSYS のパス変換が効かない**。cmd は
  `/c/prog/w64devkit/bin/gcc` を実行できないので、`/` で始まる引数は
  自分で `cygpath -w` する
* `echo %ERRORLEVEL%> "file"` は展開後 `echo 0> "file"` になり、
  **`0>` が標準入力のリダイレクト**として解釈される。echo は引数なしになり
  "ECHO is off" をコンソールに出し、ファイルは空になる。
  **リダイレクトを前に置く**: `>"file" echo %ERRORLEVEL%`

## 経路探索 (00405000) と命令の適用 (00423cc0) — 実装済み

**ここがゲームの中心だった。** 0041a680 が塗った距離場を、00405000 が
**目的地から勾配を降りて**ルートに変える:

* `cost = cells[目的地].cost`、0x1ef 超なら到達不能で 0 を返す
* ユニットが既にそこに居れば 10
* `at14 = cost`（歩数）、`at18 = 0`（進捗）
* 目的地から、コストのより小さい隣へ移りながら
  **`route[新しいcost] = 逆向きの方向`** を書く。ユニットは cost 0 から
  歩き始めるので、`route[at18]` を読んで進めば目的地に着く
* 方向の反転は `+ 4 & 7`。近傍の走査位置 `probe` は**呼び出しを跨いで
  持ち回らない**が1回の探索中は保持される（4方向を回して最初に見つかった
  下り坂へ）

### 方向表 (0x434434)

`table[dy*3 + dx]`（自分のアドレスを中心に）:

```
        dx=-1  0  1
 dy=-1:   1   2   3
 dy= 0:   0   5   4
 dy= 1:   7   6   5
```

kStepDx/kStepDy と完全に一致する — 0 が西、2 が北、4 が東、6 が南、奇数が斜め。
**中央の 5 は詰め物**で、南東の 5 と区別が付かない。(0,0) は起きないので
実害はないが、**「5 は停止」ではない。5 は南東**。一度そう書きかけたが誤り。
「ルート歩数の 5」の件は、0041d690 が経路を持たない相手に返す既定値が 5
（＝南東）というだけで、特別な意味は無いというのが今の理解。

00405000 が作るルートは4方向しか使わないので、出てくる値は 0/2/4/6 のみ。

### 付随する2つ

* **004056f0** `simUnblockTarget` — 命令の**目標セル自体**の封鎖を解く
  （地形 0x7a, 0x7b, 0x30..0x5f のとき）。壁を壊しに行く命令は、その壁に
  到達できないと成立しないため
* **004051b0** `simShortenRoute` — ルートを1歩短くする。
  **命令 6,7,9,10,11（建設・整地・撤去系）は目標セルの隣で作業する**ので、
  手前で止まる

### 選択と命令 (0040a020 / 00409e90 / 00409f10 / 00409f90 / 00423cc0)

* **0040a020** `simSelect` — 自勢力・非戦闘中が条件、force=0 なら
  既に命令済み(at0d bit4)を弾く。通ると `flags21c |= 1`、**`at220 = 0`**（吹き出し）、
  `at0e = at0c`（向きを退避）、`at0c = 6`（選択中の姿勢）
* **00409e90 / 00409f10** `simSelectAll(force)` — セルを掃いて占有者を選ぶ。
  リーダーは除外。エンティティ配列ではなくセルを掃くのは、盤上に居ないものは
  選べないという意味
* **00409f90** `simClearSelection` — 向きを戻し、吹き出しを消し、ビットを落とす
* **00423cc0** `simOrderSelected` — 選択中の各部隊に:
  * 命令コード = `DAT_004365d8`、引数1で `|0x40`、2で `|0x80`
  * 非リーダーは `|0x10`、**リーダーは `(code & 0xfd) | 0x0d` に強制**
    （0x0d = 名前表の "I'm Leader"）
  * `target[0..1] = 目的地`、`flags &= ~0x0c`
  * 00405360 → 004056f0(目的地) → **0041a680(ユニットの位置から)** → 00405000
  * 建設系なら 004051b0 で1歩短く

### **命令のスイッチは「経路が無いとき」しか回らない**

00403170 は全体が `if (+0x18 == 0x1f0)` に包まれている。**移動中の部隊は
命令の処理を素通りして歩くだけ**。これを落とすと、遠くへ送られた部隊が
1ティック目に足元で命令を実行しようとし、何も無いので命令を捨てる —
つまり**誰もどこにも到達しない**。移植で実際にこれが起きて、歩行テストが
捕まえた。

エンティティの分岐も原作を再確認した（19570行付近）:

```
flags & 2        -> 00420af0（死亡中）
faction == 4     -> 00402700（中立）
at0d & 0x20      -> 00401000（リーダーの歩行）
!(at0d & 0x10)   -> 00401770（命令なし）
!(flags & 4)     -> 00403170（命令の実行）
それ以外          -> 00402bc0（別の分岐、未移植）
```

**flags bit2 が 00403170 と 00402bc0 を選び分ける**。00423cc0 は命令を与える
ときに `flags &= ~0x0c` するので、命令直後は 00403170 側。00402bc0 の方は
まだ移植していない（case 4 が獲物を探して経路を引き直すなど、内容が違う）。

**`at220` の 1..4 の正体（解決）** — 004237e0 が書く。**「そこへ行けるか」の答え**で、
3回、だんだん雑に試す:

| 塗り方 | 届いたら | 意味 |
|---|---|---|
| 00405510（危険も避ける） | **2** | 安全に行ける |
| 00405440（**通れないものだけ**） | **3** | 危険を通れば行ける |
| 何も塗らない | **4** | 味方も押しのければ行ける |
| どれも駄目 | **1** | 行けない |

00423cc0 は **1 と 0xff の部隊には命令を出さない**。3 と 4 のときは
`DAT_00434544` を立てて窓を開く（＝「危ない道ですが良いですか」の確認）。

これは**選択中にポインタを動かしている間**の表示だった（00423940 が選択中の
全部隊に対して呼ぶ）。頭上の吹き出しが、部隊ごとに「そこへ行けるか」を
リアルタイムで答える。移植では `simReachTarget` / `simAimSelection`、
WASM は `lm_aim(x, y)`、ページはポインタ移動時に呼ぶ。

**00405440 は 00405510 から判断を抜いたもの** — 同盟と吸収できない味方だけを
塞ぎ、危険については何も言わない。

## 状態の吹き出し (0041f790) — 実装済み

毎ティック、**自軍のユニットだけ**に吹き出しを付け直す。他国のは付かない。

| 番号 | 条件 |
|---|---|
| 10, 11 | 命令を持っている（`flags & 4` か `at0d & 0x10`） |
| 8, 9 | **資金不足**（`DAT_0043451c` が立っていて、命令が 5/6/7） |
| 6, 7 | 戦力100未満で命令が 2（その場に建てろと言われて力が足りない） |
| 12..15 | **リーダー**が、経路を持っている（＝城を離れている）か勢力フラグ bit1 |

番号が対になっているのは `(frame >> 1) & 1` で交互に切り替えるため（リーダーは
`& 3` の4コマ）。**後の判定が勝つ**ので、資金不足は命令中の表示を上書きする。

`DAT_0043451c` は 0041ec60（資金確認）が失敗したときに -1 になり、
エンティティ掃引の頭で 0 に戻る。**これが「お金が足りない」を伝える唯一の手段**。

移植で1つだけ足した: **選択中(`flags21c & 1`)の部隊は上書きしない**。
原作は選択中ゲームが止まるのでこのパスが走らないが、移植では裏で動き続けるため。

## 自分で対象を探す (00402bc0 と探索群) — 実装済み

**flags bit2** が立った部隊は 00402bc0 に行く。ビットが立つのは1箇所だけ:
**自軍のユニットが生まれるとき**、保留中の命令 `DAT_004365e0` が 1 でなければ
`flags |= 4` とその命令が刻まれる（180行付近）。

命令メニューの表 (0x434444) は **{1, 4, 5, 8, 9, 0x0b}** の6種で、各3変種:

| 変種 | 加算 | 意味 |
|---|---|---|
| 0 | なし | **0x10 が付かない**ので命令ハンドラに行かない（好みとして持つだけ） |
| 1 | `\| 0x50` | 0x10（命令として有効）+ 0x40 |
| 2 | `\| 0x90` | 0x10 + 0x80（追い続ける） |

`pendingOrder` は**合成済みのバイトをそのまま**持つ。WASM 側の
`lm_set_order(n)` が `n | 0x10`（変種1相当）を作る。

### 00402bc0 の形

各 case が同じ3段構え:

1. **その場でやってみる**（できたら終わり）
2. `!(flags & 8)` なら**近くを探す** → 見つかれば 00405250 で近傍ルート、
   **`flags &= ~4` して 00403170 側に引き渡す**（到着後に実行される）
3. 0041eb60 の**作業許可**が取れれば**もっと広く探す** → 取れなければ諦め

### 探索は同じループの6回書き（一本化して移植）

0041dfb0 / 0041e0a0 / 0041e1d0 / 0041e360 / 0041e480 / 0041e560 は
**判定だけが違う同じ走査**。近傍は 0x434400/0x434410 の
**index 1..4 = 東西南北、5..8 = 同じ方向の2マス先**。

| 探索 | 命令 | 受け入れる地形 |
|---|---|---|
| 0041e560 | 休止時 | 自国のユニットセル (8+f) |
| 0041e0a0 | 4 | 他勢力のユニットセル (8..0xb、自分と同盟以外) |
| 0041e1d0 | 5 | 空き地 (0) か自国領 (0xc..0xf)。**先に資金100の確認** |
| 0041e360 | 8 | 建物 (1..4)。**命令ビット無しかつ戦力100未満なら探さない** |
| 0041e480 | 9 | 壁 (0x7b) |
| 0041dfb0 | 0xb | 中立の湧き点 (5) |

**角抜けの禁止**が効いている: マスクを毎回右シフトし、閉じたセルで bit7 を
立てる。4歩後に bit3 として戻ってくるので、**2マス先は間の1マスが塞がって
いれば見に行かない**。

### 作業許可 (0041eb60) と `DAT_0043781c`

0040a5e0 が毎ティック **4** を入れ、0041eb60 が1つ消費する。取れた部隊は
flags の bit0/bit3 が下り、取れなかった部隊は **bit3 が立つ** —
これが探索を始める前の `flags & 8` の正体。**1ティックに4部隊しか
重い探索をしない**という負荷分散。

### 移動の可否 (0041e700)

* **そのセル**: 同勢力なら合計が10万を超えなければ吸収して入れる、
  同盟国のには入れない、**自分以下の相手には入らない**
* **周囲4マス**: 止まっている敵が自分より大きければ避ける、
  動いている敵は**こちらに向かって来ていて、かつ大きいときだけ**避ける
  （＝素通りする敵は無視する）

**未読のまま残っているのは「もっと広く探す」側** — 00421050, 00422290,
00422370, 00422460, 00422530。移植では諦めて次のティックに回している。

## 広域探索 (00422290 系) — 攻撃だけ実装済み

```
0041a9f0(entity, col, row):        # その部隊にとっての距離場
    at18 = 0x1f0
    00405360()                     # 全部リセット
    00405510(entity)               # 危険を塗る（下記）
    0041a680(col, row)             # 自分の位置から塗り広げる

00422290():                        # 命令4の広域探索
    0041a9f0(...)
    0041cc30(&out) で**最も近い敵のユニットセル**を探す（コスト最小）
    if 自分の戦力 < コスト * 2: 諦める     # ← 遠征の可否はこれだけ
    target = out; 00405000 でルート
    at0d = (at0d & 0xd4) | 4; at0f = 4
```

### 危険の塗り方 (00405510)

**止まっている**エンティティだけが対象（動いている相手はそこに居ないから）。

| 相手 | 判定 | 塗り |
|---|---|---|
| 同盟国 | 常に | **そのセル自身** |
| 同勢力 | 合計が10万超（吸収できない） | **そのセル自身** |
| 自分の城に居る敵 | 戦力×2（**リーダーなら×4**）が10万超、または自分がそれ未満 | **周囲4マス** |
| ふつうの敵 | 戦力 > 0xcccc、または自分が戦力の1.25倍未満 | **周囲4マス** |

**「周囲4マス」はセル自身を塗らない** — だから危険な相手には**突っ込めるが
すり抜けられない**。うまい設計。

### 目標選び (0041cc30)

全セルを見て、**他国のユニットセル(8..0xb、自分と同盟以外)のうち塗った
コストが最小**のもの。到達不能は 0x1f0 なので自然に除外される。

### 5つとも同じ形（一本化して移植済み）

| 広域探索 | 命令 | 目標を選ぶ関数 | 探すもの | 前提 | 成立後の at0d |
|---|---|---|---|---|---|
| 00422290 | 4 | 0041cc30 | 他国のユニットセル | — | `(at0d & 0xd4) \| 4` |
| 00421050 | 5 | 0041c690 | **空き地(0)** で自国/同盟の集落に隣接しない | 資金100 | `(at0d & 0x95) \| 5` |
| 00422370 | 8 | 0041cce0 | 建物 (1..4) | 戦力 >= 0x14 | `(at0d & 0xd8) \| 8` |
| 00422460 | 9 | 0041c800 | 壁 (0x7b) | — | `(at0d & 0xd9) \| 9` |
| 00422530 | 0xb | 0041cd50 | 中立の湧き点 (5) | — | `(at0d & 0xdb) \| 0xb`、004051b0 で1歩短く |

全部に共通: `0041a9f0` で自分向けの距離場 → 最小コストのセル →
**`戦力 < コスト * 2` なら行かない** → `00405000` でルート → `at0f = 4`。

**壁だけ扱いが違う**: 目標は壁そのものだが、**歩く先は壁の隣**（そこから
壊すため）。0041c800 は壁ごとに4近傍のコストを見て一番安い隣を選ぶ。

**建てる場所の判定 (0041e670)**: 候補の**3×3以内に自国か同盟の集落があれば
不可**。simBuildUnitCell の拒否条件と同じ規則。

### 実マップで分かったこと

B_003 の 1000掃引時点で、**自軍のどの部隊からも敵のユニットセルに到達できない**
（塗った到達域は404セルで、そこに敵地が無い）。危険塗りのせいではなく
**地形そのもの** — 各国が山などで区切られた区画に居る。ロードモナークらしい。
だから狩りのテストは合成マップで行う（開けた盤で 4,4 から 30,30 へ52歩）。

## 占有率 (0041b640) とリーダー戦力 — 実装済み

`+0x34` は**このゲーム唯一の float**で、盤面の占有率（小数2桁）。

* **0041cdf0** = 取り合いの対象になるセルの総数。地形 0..4（空き地と建物）、
  8..0xf（ユニットセルと領地）、0x7b（壁）**だけ**を数える。山や水は数えない
  ので、山だらけの面でどの国も小さく見える、ということにならない
* **0041ceb0(f)** = その国のセル数（地形 f+8 と f+0xc）
* **0041b640** = `100 * 自国 / 総数` を1/100単位に丸めて4カ国分書く。
  **毎ティックではなくパネルのために呼ばれる**

Graph Window が出す1国あたり2行はこれで全部揃った:
`"%s Funds: %d"` / `"%s Area Occupied: %3.2f"` /
`"%s Leader Strength: %d"`（**`factions[f].at0c` が指すエンティティの戦力**）/
`"%s Unit Totals: %d"`（+0x10）/ `flags & 0x40` なら `"%s Defeated"`。

### `at08` と `at0c` は誰も書かない（移植側で埋めた）

**`factions[f].at08`（首都の座標）も `at0c`（リーダーの番号）も、
792関数のどこからも書き込まれない** — セーブ/シナリオの読み込みだけが値を入れる。
新規開始の面では両方0のままなので、移植では `simSeedLeaders` が城を置くときに
**両方を埋める**。これは原作にはない、こちらの補い。

ページのパネルは資金・税率・**占有率**・戦力・**王の戦力**を出す（王の戦力は
国の戦力に含まれない、が原作の仕様）。

## Unit Window の中身 (00426900) — 読解済み、ページに移植

カーソルの下のセルを見せるパネル。

* 地形タイルを**32pxバンクから**そのまま貼る（`terrain << 10` = ×1024）
* **ユニットセル(8..0xb)なら `"NUM:%6d"`、それ以外は `"DEF:%6d"`** —
  どちらも**セルの `+0x0c`（value）**。ゲージの長さは
  NUM が `value * 0x1e / 0x90`、DEF が `value * 0x1e / 0xff`（上限30）
* ユニットセルのときは**選択中の待機命令の名前**も出す
  （`&DAT_00435b86 + (DAT_004365e0 & 0xf) * 0x11` = 名前表のレコード 6+n ✓
  これで命令名の並びが裏付けられた）
* ゲージは1目盛ずつ伸縮するアニメーションで、絵は UIシートの
  `((n/10 + 4) * 0x200 + n%10) * 8` から 8x16 を貼る
  （＝行 8以降、列 0..79）

移植ではページ側に「CURSOR」欄として出した（マス座標・中身・DEF/NUM・部隊）。
絵のゲージはまだ描いていない。

## 国の滅亡と勝敗 (0041f090 / 0041f0d0 / 0041f4c0) — 実装済み

ティックの残り4つのうち3つが読めた。

* **0040a110** — **戦況で曲を切り替える**。他3国の戦力を合計して、
  自分がそれより弱ければ**面の1曲目**、合計の**8倍以上**あれば**2曲目**。
  その間は**今かかっている曲をそのままにする**ので、曲は戦況が動いたときだけ変わる。
  曲は背景セット番号そのもの（set 10 なら LM010/011/012）
* **0041b4f0** — 偶数フレームで経過時間 `DAT_004365c8` を +1（999999で頭打ち）、
  `DAT_004365c4` を -1。時計
* **0041f090** — 4勢力を見て、**まだ脱落していない(0x40が無い)のに
  bit0（リーダー喪失）か bit4（戦力0）が立っている**国を 0041f0d0 で滅ぼす
* **0041f0d0** — 滅ぼす処理:
  * bit0 が無ければ（＝王がまだ生きていれば）**王を殺す**
  * **首都の3×3を地形0に戻す**
  * **残った資金を `+0x1f` が指す国へ渡す**（10万で頭打ち）— 国を取ると儲かる
  * 自分の資金と税率を0にし、**flags |= 0x40**、00405330 で通行判定を作り直す
* **0041f4c0** — 面の終わり。**4ティックに1回 0041b640（占有率）を計算**し、
  `0x40|1|0x10` が揃った国を数える。プレイヤーが脱落していれば負け、
  脱落しておらず他の3国が脱落していれば勝ち。
  （原作はさらに「どのセルの `+0x14` も0であること」を条件にしている。
  カーソルが入る場所なので意味が分からず、移植では省いた）

### **.MAP だけでは面として成立しない（3つ目の証拠）**

**15面のうち7面は、どの国も城だけで集落を1つも持たない**（B_000, B_002,
B_105, S_101, S_115, S_201, T_000）。0041b370 は集落(8+f)しか国力に数えず、
**王の戦力は数えない**ので、その7面では開始時に全員が戦力0 → 0041b370 が
印を付け → 0041f090 が初回ティックで全滅させる。**字義通りだと始まる前に終わる**。

`at08`（首都）も `at0c`（王）も誰も書かない件と合わせて、結論は同じ:
**原作は .MAP のほかにシナリオを読み、実体と勢力レコードを供給している**。
移植にはそれが無い。

そこで `simSeedLeaders`（元々この穴を埋めるための関数）が、
**集落を持たない国には城の近くに1つ置く**ようにした。プレイヤーが最初に
やることと同じ。**これも原作にはない、こちらの補い**。

なお `stateStartStage` からは敗北の印付けを外した。原作は面の読み込みの
最後で印を付けられる（その時点でシナリオが実体を置き終えているから）が、
移植ではリーダーを後から置くので、そこで印を付けると全員が条件を満たす。
印付けはティックがやる。

## AI は 00401770 の中にある

**別建ての AI ルーチンは無い**。コンピュータの国も人間と同じ規則で動き、
違いは「プレイヤーが命令を出すかどうか」だけ。自律行動の中身は
**00401770（命令を持たないユニット）** に入っている — 3931バイト、409行で、
`+0x0d` への書き込みが24箇所。**ユニットが自分で自分に命令を出す**。

### 004219b0 = 仕事探しの優先順（実装済み）

近傍探索6種を**この順**で試し、最初に見つかったものを命令にして歩き出す:

| 順 | 探すもの | 付ける命令 |
|---|---|---|
| 1 | 中立の湧き点 | 0x0b |
| 2 | 敵 | 4 |
| 3 | 建てる場所 | 5 |
| 4 | 建物 | 8 |
| 5 | 壁 | 9（**コンピュータの国だけ**） |
| 6 | 自国の地面 | 1 |

0x0b と 9 は 004051b0 で1歩手前まで（隣で作業するため）。

### 00421ae0 = コンピュータの戦略（実装済み、2つを除く）

**コンピュータの国だけ**が入る。近くに仕事が無いときの落とし先。

| 順 | ルーチン | 中身 |
|---|---|---|
| 1 | 00420ef0 | 他国が「見つけた地点」(勢力+0x20/0x21)へ。**未移植** — 00421660 と地形 0x20..0x2f の意味が未読 |
| 2 | **00421140** | **王への合流**。自国 flags に 0x20（王が助けを求めた印）があれば王の位置（動いていれば行き先）へ |
| 3 | **00421270** | **敵の王を狩る**。自分と同盟以外のリーダーのうち最も近いもの。**戦力1000超のときだけ** |
| 4a | 00421d30 | 汎用の目標選び (0041c410)。**未移植** |
| 4b | **00422040** | **大きい味方と合流**して1つになる (0041caf0 = 自分より重く、合計が10万未満で最も近い味方) |
| 4c | **00422110** | **自国の集落へ帰る** (0041cbc0)。既に集落の上なら止まって命令2（そこに建てる） |
| 5 | **00421f20** | **地図に無作為に矢を放つ** — 空き地(0)で、半分の重さで届いて、自国の集落が1マス以内に無ければ建てに行く |

4a-4c は乱数 > 0x59、5 は > 0x50 のとき。**国境が接していない土地へ広がるのは
00421f20 の役目**。

### 実際に決着するようになった

B_004 を60000掃引: **3国が脱落し1国が残る**。ただし勝者は中立の魔物に
削られてボロボロで、**魔物(勢力4)が61体まで増えて損害18328を与えている**。
湧き点を潰さないと飲まれる、というこのゲームらしい結末。

### 平時ユニットも経路を歩く

00401770 も 00403170 と同じく全体が `if (+0x18 == 0x1f0)` に包まれていて、
**経路があるときは歩く**。移植ではここを「リーダーだけが歩く」と誤解して
素通りさせていたため、**仕事を探しても現地へ行けず、国が広がらなかった**。
繋いだ結果、B_003 で3000掃引のうちに集落 41→75、領土 114→221 に伸び、
資金も実際に減るようになった（税率が上がって釣り合う）。

到着後の作業も平時ユニットの分岐に入れた（1/2/3/5 は建てる、8 は壊す、
9 は壁、0xb は湧き点）。**平時ユニットの `+0x0d` は命令ではなく「何をしに
来たか」**で、着いたらそれをする。

## 地形 0x20..0x2f = 鉱脈（解決）と、王の行動 (00401000)

`0040b960`（鉱山を作る）の前提が `(地形 - 0x20) <= 0xf` なので、
**0x20..0x2f は掘れる鉱脈**。掘り切ると 0x7a（鉱山）になる。
`0041f3f0` は「0x30..0x5f（風景）／0x20..0x2f（鉱脈）／0x7a（鉱山）」を
まとめて障害として扱う判定。

これで **00420ef0 の前提も分かった**: 王が城を離れている（勢力 flags bit1）
状態で鉱脈の上に立つと、**その座標を勢力+0x20/0x21 に記録して flags bit2 を
立てる**（＝国に知らせる）。00420ef0 は**他国**のその記録を見て向かう。
記録する側は移植した。向かう側（00420ef0）は未移植のまま。

### 00401000 は2枝に分かれる（**移植を訂正**）

```
経路が無い: 0041ef80（反撃）→ at0c = 6 → コンピュータの国だけ王の判断
経路がある: 鉱脈の発見を記録 → 向きを合わせる → 1歩進む
```

**歩行は「経路がある」側にしか無い。** 移植では経路が無いときも
0041d690 の既定方向（5＝南東）へ歩き続けていた — **誤り**。王は経路が
無ければ南を向いて立っている。テストがこの誤挙動を前提にしていたので、
経路を与える形に直した。

王の判断（**コンピュータの国だけ**）:

* **00421660** — 城へ帰る。到達可能で、王が距離の2倍以上の重さがあれば。
  成功したら勢力 flags の bit1（外出中）と bit2（報告あり）を落とす
* 帰れなければ **flags |= 0x20（助けを呼ぶ）** して 00422110 で集落へ。
  この 0x20 が 00421140（王への合流）の合図

## 00401770 の分岐の形（移植し直した）

各 case は同じ形をしている:

```
その好みの作業をする
終わった/できなかったら:
    at0d = 1                      # 素の好みに戻す
    0041e560 で自国の地面が見つかればそこへ
    見つからなければ at0c = 6 で南を向いて待つ
```

**004219b0（仕事探し）と 00421ae0（戦略）を呼ぶのは case 1/2/3 だけ**。
他の case は作業が終わったら自国の地面へ帰るだけ。移植をこの形に直した。

case 5（建てる）だけ例外で、**建てられたら次の建設地を探す** — これが
1か所に固まらず国が広がる理由。

### 湧き点つぶしは動く（テスト済み）

0040bb10 は目標セルの湧き点から**自分の戦力の1/8**を毎ティック削り、
0になると地形 0x60（風景）に変わる。合成マップで確認済み。

### 開始条件の注意（移植の弱さ）

集落を持たない7面では `simSeedLeaders` が**1つだけ**集落を置く。
原作のシナリオがどれだけ与えるかは分からない。**1つでは弱すぎて**、
B_000 では6000掃引ほどで中立の魔物（湧き点6個）が53体まで増えて4国とも
滅びる。移植した規則の帰結であって規則の誤りではないが、**開始条件が
原作より弱い可能性が高い**ことは意識しておくこと。

## MAP/NAME.TXT = キャンペーンそのもの

小さな ini。`[entry]` に `QESTNAME=` と `MAP=`（面数）、続いて `[0]`, `[1]`, ...
に `NAME=`（**ゲームが表示する面名**）と `FILE=`。`//` 行はコメントで、
英語版には日本語の原題が残っている。

**この並びが実際のプレイ順で、ファイル名の辞書順ではない。**
移植は辞書順だった（誤り）。正しい順:

```
0 Conqueror's trial  B_000    8 Geo Port Flash    S_115
1 Backroad Euromap   B_003    9 Triple Attack     B_009
2 Ancient Kingdom    B_004   10 Forest War        S_101
3 Steel Brain        B_006   11 Number One!       T_000
4 Candy Waltz        S_201   12 Sleeping Dragon   S_105
5 Strange Islands    B_104   13 Space Colony      B_105
6 Precious Nature    B_002   14 Moon and Stars    B_103
```

`worldReadStages()` が読む。ファイル名は小文字で書かれているが書庫は大文字
なので大文字に直す（ホストは名前の末尾で照合するため）。日本語版の面名は
Shift-JIS なので、ページ側で他の名前と同じように復号する。

## DATA/*.256 の絵（実装済み）と、パレットの並びの訂正

| ファイル | 大きさ | 中身 |
|---|---|---|
| LOGO | 256x192 | ファルコムのロゴ（青空と太陽） |
| GAKU, GAKU1..4 | 352x304 | 幕間の絵5枚 |
| END | 512x384 | エンディング |

形式は素直: little-endian の幅・高さ、そのあと1バイト1画素。

**.PAL の並びは (R, G, B, 予備) で、BGR ではない。** 名前が RGBQUAD なので
青が先だと思い込んでいたが、**エントリ9が 166,202,240（Windows標準の空色）**
で、そう読まないと合わない。逆に読むと絵が全部モモ色になる。
`src/picture.c` と `tools/dump_assets.py` の両方を直した。

WASM は `lm_picture_open/width/height/pixels`、ページは「絵」ボタンで
7枚を順に出す。zip を読んだ直後にロゴが出る。

## 王が城の上でやること (004015a0) — 実装済み

00401000 の頭で毎回呼ばれる。**自分の城の上にいるときだけ**動く:

* **国力 < 王の戦力の半分** なら、**王の戦力の1/4を割いて城の1つ南に新兵を置く**
  （プレイヤーなら待機命令も刻む）。**国が兵を失ったときの補充はここから来る**
* 逆に**国力 > 王の戦力**なら、王が**差の1/8を吸い上げる**（10万上限）。
  豊かな国は王を太らせる

## 王が城から出る (00401000 の城分岐) — 実装済み

自分の城の上で、**南2マスが空いていて歩ける**なら1マス南へ出て、
そこから 00405250(-3,-3) の経路を取る。**王が盤上に出る唯一の道**。

### 経路表の 5 は詰め物（訂正の訂正）

`kRouteSteps` は1行3歩で、歩数 `at14` の分だけ歩いて止まる。だから
1歩の経路 `{0,5,5}` の 5 には**届かない** — あの 5 は詰め物。
`kStepDx` の 5（南東）とは別の表の話で、両者は矛盾しない。
ただし at14 が3を超えると詰め物を踏んで行を飛び出すので、**at14 は3で頭打ち**に
した（00401000 が (-3,-3) を渡してくるため）。

## 自分の集落を拾う (00421910) — 実装済み

**自国の集落セルの上に立った部隊は、そのセルの価値を自分の戦力に取り込み、
セルを更地（価値100）に戻す。** 命令4（攻撃）の処理は**まず これを試す** —
自分の集落の上を歩いて帰るのが軍を集める方法だから。

これで「土地 → 軍 → 土地」の循環が閉じた:
`0040b330`（部隊を削って集落を作る）の逆向きがこれ。

## 小関数、読んだもの

* **0041a800** `simReroute`（実装済み）— 今の目標へ**経路を引き直す**。
  00403170 の歩行は、我慢(+0x0f)が尽きたとき**すぐには諦めず**、
  作業許可を取ってこれを試す。通れば続行、駄目なら命令を捨てる。
  **戸口で詰まった部隊が回り道をする**のはこれ
* **0041eea0** `threatAround`（実装済み）— あるセルの4近傍にいる敵の戦力の合計を
  自分と比べる。**8 = 誰も居ない、7 = 自分の方が強い、9 = 相手の方が強い**
* **0041ec30** — 一歩の範囲判定（`stepInBounds` として移植済み）
* **0041ac10** — マンハッタン距離
* **0041e920** — 読んだが**未移植**。リーダーが首都と同じ列に居るときの動きと、
  **今の向きから4方向を順に見て、空いていて 0041e700 に断られない方を向き、
  そこへ殴りかかる**（`at0c = (i & 3) * 2`、`at18 = 0x1f0`）。
  コンピュータのユニットが危険なセルに立っているときの詰み回避
* **0041f020** — 「その一歩が空いているか」（範囲内・地形 <= 0x2f・占有者なし）

王が城を出る条件は `0041eea0` を2回使う: **1マス南は「自分より強い敵に
囲まれていない」こと、2マス南は「自分より弱い敵が居る」こと**。
つまり**王は用も無いのに城から出ない** — 叩ける相手がいるときだけ出る。

## セーブ形式 (0x16218) — 実装済み

**無圧縮**。`FUN_0040c010(path, buf, 0x16218)` がそのまま読み、
**勢力レコード(0x118) → セル(0xd800) → エンティティ(0x8900)** の順に
コピーするだけ。ヘッダも区切りも無い。

構造体はバイト単位で原作と同じ（`tests/state_test.c` の先頭の offset 検査が
それを守っている）ので、**原作が書いたセーブがそのまま読めるはず**だし、
こちらが書いたものは原作でも読めるはず。

**どの面のセーブかはファイルに入っていない** — 原作も別で持っている。
ページは localStorage に面番号と操作国を添えて保存する。

## SOUND/SOUND.CFG = サウンドトラック表

NAME.TXT と同じ ini。94項目。各ブロックが `ENTRY=`（**そのままファイル番号** —
entry 10 は LM010.MID）、`NAME=`（曲名）、`MIDI=`、`LOOP=`、`CD=`（CD版の
トラック番号）を持つ。

**曲名が仕掛けを裏付けている**: 各背景セットに **Normal / Happy / Pause** の
3曲があり、0040a110 が選ぶのはまさに Normal と Happy。

```
entry  0 Ending Theme (LOOP=0)     entry 10/11/12 Lord Monarch Night (set 10)
entry  2 Winner!                   entry 20/21/22 Food Panic        (set 20)
entry  3 Game Over                 entry 30/31/32 China Wars        (set 30)
entry  4 Award(class)              ...
entry  6 Days Passed
```

末尾の項目（entry 05007 など）は**効果音の名前だけで MIDI ファイルが無い** —
"Build Bridge"、"I've lost..."、"Giddyup!"、"SIGH"、"Magic Sound"、"Bird"。
**この版に効果音は入っていない**（CD版か別デバイス用）。音楽だけ。

`worldReadTunes()` が読み、WASM は `lm_tune_name` / `lm_tune_loops`。
ページは曲が変わるときに曲名を出す。

## インターフェイスの絵の位置（測定済み、未使用）

`lm_ui_region(x, y, w, h)` で**シートの任意の矩形**を UI の色つき RGBA として
取り出せる（0x70 は透明で返る）。これで原作の部品を好きな場所に描ける。

分かっている座標:

| 部品 | 位置 |
|---|---|
| 数字（大白/大赤/小白/小赤） | (0,0)/(0,16) が 8x16、(0,32)/(0,48) が 4x8 |
| **ゲージ 30個** | 行 `(n/10 + 4) * 16`、列 `(n%10) * 8`、8x16。**10個ずつ青→緑→黄** |
| 命令の吹き出し | x = `0xc0 + タイルサイズ`、y = `番号 * 32` |
| カーソル/格子 | x = `0x78 + タイルサイズ`、y = 16 |
| **176幅のパネル** | x 0..175、y 110 あたりから**96行のものが2枚縦に並ぶ**。
  各パネルは左に縦長の枠1つ、右に横長の枠3つ |
| 飾り枠（青と金） | 行 960..1023、幅256 |
| 吹き出しの下地 | 0x20000（行512）から 208x64 |

### Unit Window の背景は**シートの行112から176行**（確定）

**組み立てて確かめた**。`tests/unit_window.exe <top> <out.ppm>` が
00426900 と同じ配置で合成する:

* 背景 = シートの (0, top) から 176x176
* 32pxの地形タイルを (16, 112)
* 数字を (64, 120) に右詰め（大・白フォント、1桁 8x16）
* ゲージを y=96、x=72 から

**top = 112 のとき**、枠が欠けず、タイルが左の縦長スロットに、
ゲージと数字が右の横長スロットに**きれいに収まる**。1行ずれると上か下から
別の絵が入る。**これが Unit Window の背景**。

96行のパネルが2枚積まれている形で、00426900 が描くのは**下側**の枠。
上側は別の情報用（未確定）。

### Progress Window の背景は**行288から176行**（確定）

176幅のパネル帯はシートの**行110〜463**に連続していて、そこに2枚ある:
**行112..287 が Unit Window、行288..463 が Progress Window**。

Progress Window は絵そのものが中身を教えてくれる — 金袋・%と天秤・太陽・砂時計。
スロットを実測した（パネル左上を原点とする座標）:

| スロット | 中身 |
|---|---|
| (24,16,128,6) と (24,26,128,6) | 上のグラデーションのバー（2段） |
| **(32,40,48,16)** | **資金**（金袋の隣、6桁） |
| **(120,40,24,16)** | **税率**（% と天秤の隣、3桁） |
| (24..152, 90, 6) | 星のバーの区画 |
| **(32,104,48,16)** | **経過日数**（太陽の隣） |
| **(96,104,48,16)** | **残り日数**（砂時計の隣） |
| (50,154,76,6) | 下のバー |

数字は大・白フォント（1桁 8x16、シートの (d*8, 0)）を右詰めで置く。
組み上げて確認済み（資金4868 / 税17 / 842日 / 残り2158）。

**Graph Window (x=352) の背景は無い（解決）** — `00404e40` を読んだ結果、
あの窓は GDI のテキストだけ。下の「Graph Window は絵が無い」を参照。

## 国が伸びて・ぶつかって・滅びるまで（決着した）

「4か国が勝手に大きくなり、戦い、どこかが滅びる」までが通るようになった。
止めていたのは3か所で、どれも移植の抜けだった。

**(1) 0041e1d0 が緩すぎた。** 集落を建てる先を探す近傍探索は、原作では
`0041e670`（自国の集落が隣接していないか）を必ず訊く。これを落としていたので、
集落で埋まった区画でもユニットは毎回「建てられる場所」を見つけ、1マス歩き、
`0040b330` に断られ、また別のマスを見つけて戻る、を延々くり返した。
ブラウザで見えていた「同じ場所を高速でうろうろ」はこれ。
ついでに原作は**他国の集落**も建設候補として受ける（自国と同盟国だけが除外）。

**(2) 00421d30 の後半を移植していなかった。** 広域探索 `0041c410` が
何も見つけないとき、原作はさらに2つ訊く:

| | 探すもの | 与える命令 | 条件 |
|---|---|---|---|
| `0041c800` | 壁 `0x7b` | 9 | なし |
| `0041c8e0` | 鉱脈 `0x7a` | 7 | 国庫に500、鉱脈の隣に**塗り残し**が1つ以上、隣に強い敵がいない |

どちらも「対象マス」と「その隣の一番安いマス」を返し、対象を `at02/at03` に、
歩く先はその隣。鉱脈のほうは比較のときだけ距離に +0x14 されるので、
同じ距離なら壁が勝つ。**閉じた地面に自力で穴を開けるのは、実行ファイル中でここだけ**。

**(3) 0041ad90 が丸ごと無かった。** 地面が開いたとき（壁を壊す・鉱脈を掘る・
湧き点を潰す・魔物が壁を食う）に、原作は毎回 `00405330` で全セルの
通行可否を引き直し、経路を持たない遊んでいるユニットの命令を 1 に戻す。
これが無かったので、**高い金を払って開拓したマスが永久に通れないまま**だった。

あわせて 00401770 の歩行分岐を専用に移植した（00403170 のものとは別物）。
閉じたマスに踏み込んだコンピュータのユニットは、そこが鉱脈なら命令7、壁なら
命令9 を自分に与えて対象に指定する。同時に `0041ecc0`（正面衝突したら道を譲る）、
`0041e920`（詰んだユニットを押し出す。王なら城の列を北へ帰る）、
`004215a0`（特定の敵に突撃）も入った。命令の switch は原作の全ケースをそのまま持ち、
**共通の末尾は無い**（作業系の命令だけが「命令1に戻って自国の集落へ」を各自書いている）。

### 全15面の結果（`tests/growth_test.exe`、操作者なし = `humanFaction = 4`）

    B_000  losses  140171, 3 out, 決着 5615     S_115  losses 1201820, 3 out, 決着 41704
    B_003  losses  280769, 3 out, 決着 7021     B_009  losses   88917, 1 out
    B_004  losses  158588, 3 out, 決着 4874     S_101  losses  221893, 3 out, 決着 6144
    B_006  losses  591689, 3 out, 決着 14329    T_000  losses    9133, 3 out, 決着 4000
    S_201  losses  750895, 3 out, 決着 28284    S_105  losses  380358, 3 out, 決着 11026
    B_104  losses   38730, 1 out               B_105  losses  162953, 2 out
    B_002  losses  495264, 3 out, 決着 19188

**B_005（Saint Steed）と B_103（Moon and Stars）だけは動かない。これは原作の答**。
両面とも国が「素の地面の区画」に分かれていて、間は風景 `0x30..0x5f`。
風景を消せるのは命令7 (`0040b680`) だけで、命令7 がユニットに渡る経路は
実行ファイル全体で2つしかない — 00401770 が鉱脈に踏み込んだユニットに渡すか、
00421d30 が `0041c8e0` の見つけた鉱脈に対して渡すか。`0041c8e0` は
`0x7a` しか探さず、この2面には `0x7a` が1マスも無い。`0041c410` も風景を数えない。
（`+ 0xd` への代入を全部 grep して確かめた。）
つまりこの2面は**プレイヤーが掘る面**で、機械が勝手に掘る移植のほうが間違い。
`growth_test` はこれを `meets = 0` として書いてある。

### ついでに直した細かいところ

* `0041d870` が育てたセルからプレイヤーのユニットが生まれるとき、命令には
  **bit 4 が立つ**（`_DAT_004365e0 | 0x10`）。落としていたので、選んだ命令が
  命令ハンドラに届いていなかった。00401770 側の生成はビットを立てない — 別物。
* セル掃引は `0041d740`。RESUME と `src/sim.c` にあった `00417380` は誤りで、
  そのアドレスはチュートリアル (`00417190`) の内側。
* 四方向の表は `00434420` の8個の先頭4個。別表を作る必要は無かった。
* ページ: 国一覧が1行ずれて印を付けていた（`mine` を代入の1行前で読んでいた）。
* ページ: **観戦ボタン**。`lm_set_human(4)` で4か国とも機械にまかせる。

## 命令の出し方は「マスを先にクリックする」(00423940) — **解決、実装済み**

移植は流れを逆にしていた。原作はこう:

1. 部隊を選ぶ（ドラッグで範囲選択、または兵士をクリック）
2. **やらせたいマスをクリックする**
3. **カーソルの位置にメニューが出る。そのマスに出せる命令だけが並ぶ**
4. 各命令はさらに3段（原作の言葉で **オート / 待機 / 「〜continue」**）

どの命令が出せるかは実行ファイルの表2枚 `0x4341af` と `0x43425f`
（11種の地形 × 16命令、**どちらかが 1 なら出せる**）が決める。原作は
`base + class*16 - order` と引くので、16バイトの行の中で命令番号が
**逆向き**に並んでいる。`src/orders.c` に取り出した。

| クリックしたマス | 出せる命令 |
|---|---|
| 空き地 / 集落 / 領地 | 0 1 2 3 4 5 6 8 |
| 建物 / 城 | 0 1 2 3 4 5 8 |
| **壁 (0x7b)** | **6 9** |
| **開通済み (0x20..0x2f)** | 0 1 2 3 4 5 8 **10** |
| 湧き点 (5) | 0 1 2 3 4 5 8 11 |
| **鉱脈 (0x7a)** | **7 10** |
| **風景・海 (0x30..0x5f)** | **7 だけ** |
| 岩 (0x60..0x6f) | 0 1 2 3 |

**つまり橋の架け方は「軍を選ぶ → 海のマスをクリック → 出てくる1つだけの命令を選ぶ」。**
B_104 (Strange Islands) の背景セットではその命令名が**そのまま「橋を作る」**。
日本語版の標準セットでは 7 が「踏切を作る」、10 が「踏切を壊す」。
命令7は原作でも "continue" 行が無い（`0x435bfd` = 命令名7 を名前で飛ばしている）。

`00423940` は `TrackPopupMenu` で**モーダル**に出すが、ブラウザでは
ブロックできないので `src/ui.c` はイベント駆動で同じ形にしてある。
simtower の shim の `TrackPopupMenu` も同じく非ブロッキングなので、
どちらの土台でもこの形になる。

### 右クリックの popup (0040a980) の中身も読めた

    Start / Pause / Restart          （状況でどれか）
    Overall Order(For new units)     0x9c7e → 待機中の部隊を全選択して状態1
    Overall Order(Override all)      0x9c7d → 全部選択して状態1
    Default Orders >                 0x9caa → 命令 1,4,5,8,9,0x0b,0 の7つ
                                              （0x434444 = 01 04 05 08 09 0b 00）
                                              中5つに オート/待機/continue の3段
    Recall Leader                    0x9cb1
    Leader Position >                0x9cb7
    Resize Map >                     Small/Medium/Large（タイル 8/16/32 でラジオ）
    Display > / System > / Cancel

`DAT_004365dc` の状態機械: 0 進行 / 1 部隊選択済みで行き先待ち / 3 メニュー中 /
4 popup 中 / 5。`DAT_004365d8` が命令、`DAT_004365e4` が Default Orders の選択。

## 文字とメニューを描く (src/font.c, src/ui.c)

原作は全部 GDI で描くので、移植側には文字を描く手段が無かった。

* **`tools/make_font.py`** が東雲16ドット（**パブリックドメイン**、古川泰之氏 2000年、
  /efont/ 保守）から `src/font16.c` を作る。半角は全域、全角は JIS X 0208 の
  1〜8区＋**ゲームが実際に使う漢字だけ**。ゲームの文字列は閉じた集合
  （両リリース全15面の国名5＋命令名16）なので **637字・約30KB** で足りる。
  BDF はリポに入れない — 取得先はスクリプトの先頭に書いてある。
* **`src/font.c`** が Shift-JIS を描く（ゲームのデータが Shift-JIS）。
  持っていない字は**中抜きの箱**にするので抜けが抜けとして見える。
  `fontInk` はシートの数字の 0 から色を読むので、隣の数字と同じ色になる。
* **`src/ui.c`** が Windows 95 の見た目でメニューを描く。パレット 0xf0..0xf6 を
  `renderPalette` が入れる（ゲームの絵は 0xf0 以上を使わない）。
  - `OrderMenu` = `00423940` の命令メニュー
  - `MenuBar` = MENU 101（System/Controls/Display/Orders/Help）。
    未実装コマンドはグレー表示、Start/Pause にチェック。
  - マップ面は `UI_BAR_H` 行下から始まる。ピクセル→セル変換は全部その分を引く。
* `lm_command(id)` がゲームの仕事のコマンドを実行し、ホストの仕事
  （Load/Save/Help など）は 0 を返して呼び側に任せる。

**試験**: `tests/menu_test.exe` が上の表を全部書き出してメニューを歩く。
`tests/font_test.exe <zip>` が全15面の文字列にグリフがあるか確かめ、PPM も書く。
`tests/frame_dump.exe <zip> <map> <ppm> <sweeps> <zoom> bar:1 menu:20,20` で
クロムを絵として確認できる（窓は開かない）。

## 次にやること: simtower_wasm の Win32 shim に載せ替える

`C:\prog\claude\simtower_wasm/shim` に Win32 の再実装がある（8000行）:

    win32_menu.cpp    490行  CreateMenu/CreatePopupMenu/AppendMenu/TrackPopupMenu/
                             SetMenu/CheckMenuItem/EnableMenuItem/GetSubMenu/DrawMenuBar
    win32_gdi.cpp    1074行
    win32_window.cpp 1397行  メニューバーは本物。クリックで WM_COMMAND を投げる
    win32_dialog.cpp  564行
    win32_control.cpp 1026行
    win32_font.cpp    136行  ただし **DejaVu Sans（ラテンのみ）**

**ユーザーの合意済みの方針**:

* メニュー・ウィンドウ・ダイアログの機構は shim を使う。
  そうするとネイティブは**本物の GDI**、WASM は shim で**コードパスが1本**になる
* 文字は shim のフォント層に**東雲を足す**（`src/font16.c` がそのまま使える）
* いまの `src/ui.c` はそのための繋ぎなので、載せ替えたら捨ててよい

注意: shim の `TrackPopupMenu` は非ブロッキング（ブラウザでは当然）。
なので原作の `iVar = TrackPopupMenu(...); if (iVar == ...)` の形は
どちらの土台でも再現できない。イベント駆動のままでよい。

## 橋は「水を開拓する」こと。水も 0x30..0x5f

風景 `0x30..0x5f` には**水も入っている**。B_104 (Strange Islands) を描かせると
青い海に緑の島、そして**木の桟橋**が渡っている — あの桟橋が開拓済み
`0x20..0x2f` のタイル。つまり:

* **命令7（整地）を水に出すのが橋を架けること。**
* `0041c8e0` は `0x7a`（鉱脈）しか探さないので、**機械の国は絶対に橋を架けない**。

だから **B_005 / B_104 / B_103 の3面は「プレイヤーが橋を架ける面」**で、
放っておくと国が出会わないのが原作の答。`growth_test` はこの3面を
`meets = 0` として書いてある。

## 略奪は相手だけでなく自分も削れる (004209a0)

`004208b0`（他国の集落を襲う）は集落を削る**前に** `004209a0` を呼ぶ。
これを丸ごと落としていた:

    toll = min(集落の value, 自分の at08)
    自国の at14 （損害）に toll を加算   ← 飽和加算。0xffffffff を先に書いて、
                                          和がそれと一致しない時だけ和を入れる
    自分の at08 <= toll なら 00420aa0 で死亡（原因 = 集落の持ち主）
    そうでなければ at08 -= toll

**村を焼くのが無料だった**のが直った。これが「1国が盤面を一方的に塗りつぶす」
現象の原因だった。集落の側に与える損害は、toll を払う**前**の at08 で計算する。

## 00421ae0 の末尾は「プレイヤーの国にも」効く

    if (人間の国 != この国) { ...機械の戦略... }     ← ガードはここまで
    if (00421ba0(0x1f0) != 1 && 00422040() != 1) 00422110();

**ガードの外**なので、仕事の無いユニットは誰の国のものでも
`00421ba0`（一番近い割の合うもの）→ `00422040`（大きいのに合流）→
`00422110`（自国の集落へ帰って建てる）に落ちる。命令名の 1 が "Auto" なのは
これのこと。移植は人間の国で即 return していたので**間違いだった**。

`00421ba0` は `0041c410` のもう1つの包み方で、`00421d30` との違いは:

| | 00421ba0 | 00421d30 |
|---|---|---|
| 距離上限 | 引数（唯一の呼び出しは 0x1f0） | なし |
| 強さの条件 | `at08 >= cost*2` を常に要求 | 壁/鉱脈の分岐だけ |
| 命令の付かない地形 | **諦める** | 命令8（壊す）にする |
| 壁/鉱脈の代替 | **無い** | ある |

**効果が大きい**: 弱いユニット（at08 <= 200）は建てた集落に自分を使い切る
（`0040b330` の返り値6）ので、**開始から数百掃引は常備軍がゼロ**になり、
集落が育ってから本物の軍が出てくる。面の決着が 1〜4万掃引から
**4000〜5500掃引**に縮んだ。

## 命令メニューの3段 = 終わった後どうするか

原作の命令メニュー (0x434444) は6命令×3段。段の違いは `+0x0d` の上位ビットで、
`00403170` が**仕事が終わった後**に読む:

| ビット | 意味 |
|---|---|
| なし | 命令1に戻って自国の集落へ帰る |
| 0x40 | `at0d = 0x10`。その場で待機 |
| 0x80 | 同じ種類のものをもう1つ探す |

`lm_set_order` / `lm_order_all` / `lm_order_at` の3つとも段を取る。

## Graph Window は絵が無い（解決）

`00404e40` を読んだ。**GDI のテキストだけ**の窓で、背景の絵は無い。
文字列は2列×8行＋合計行:

| 位置 | 行 |
|---|---|
| +0x528 の8行、y 0x10..0x90、x 0..0x40 | 偶数行 `"%s Area Occupied: %3.2f"`、奇数行 `"%s Funds: %d"` |
| +0xd28 の8行、y 0x10..0x90、x 0x40..0xb0 | 偶数行 `"%s Leader Strength: %d"`（`entities[faction.at0c].at08`）、奇数行 `"%s Unit Totals: %d"`（faction+0x10）。国が倒れていれば `"%s Defeated"` |
| +0x1528、y 0x90..0xa0 | `"%s Unit and Base Totals: %d"` |

ページの国一覧が出しているのと同じ情報。**RESUME の「背景だけ未確定」は
「背景は無い」で解決**。

## 部隊の選び方（ページ）

原作にあってこちらに無かったのが**兵士を選ぶ操作そのもの**。
`lm_select_at` / `lm_select_rect` / `lm_unit_here` を足して:

* 地図をドラッグ → 範囲内の自軍を全部選択（点線の枠を描く）
* 自軍の兵士をクリック → その1部隊
* 選択中に地図をクリック → 命令ボックスの命令でそこへ送る
* 何も選択せず空き地をクリック → 集落を置く
* Shift → 選択に追加

どちらの選び方も `force = 1`（`00409e90` 相当）。命令中のユニットも拾わないと、
一度送った部隊は二度と選べない。

## 原作はマウス専用。メニューの全項目（.rsrc から実測）

「キーだけで操作できるか」を実行ファイルで確かめた。**できない**。

* アクセラレータ表は**1個だけ** — `I` -> コマンド 40001。他にキー割り当ては無い
* 792個の関数のどこにも `VK_LEFT`/`VK_UP`/`VK_RIGHT`/`VK_DOWN` (0x25..0x28) との
  比較が**1つも無い**
* カーソルを動かす唯一の関数 `0040b270` はキー入力ではない。`00424520` が
  他国で何か起きたときに 0xcc のカーソルをそこへ立て、`0040b270` が
  1ティック1マスずつ滑らせる**演出**。RESUME と `src/state.c` に
  「原作はキーボードで動かす」と書いてあったのは誤りで、直した

メニューバーは普通の Windows メニューなので Alt+英字でメニュー操作はできる。
`.rsrc` の MENU 101 の中身（コマンド ID つき）:

    [System]   Load(40051) / Save(40021) / Load Map[Quest Map(40020), Single Map(40117)]
               Restart(40110) / New(40114) / Quit(40044)
    [Controls] Start(40045) / Pause(40030)
               Leader Position[????(40080), 1(40081), 2(40082), 3(40083)]
               Resize Map[Small(40048), Medium(40049), Large(40050)]
               Alliance(40012) / Customize System(40033) / Customize Sounds(40116)
    [Display]  Hide Title Bar(40108) / Hide Tool Bar(60005) / Float Tool Bar(40109)
               Unit Window(60001) / Progress Window(60002) / Graph Window(60003)
               Set Windows to default(40111)
    [Orders]   Overall Order(For new units)(40062) / Overall Order(Override all)(40061)
               Recall Leader(40113) / Default Orders(40038)
    [Help]     Help(40037) / Version(40055)

移植の対応:

| メニュー | 移植 |
|---|---|
| Load / Save | `lm_save` / `lm_load`（localStorage）|
| Load Map / Restart / New | 面の前後ボタン |
| Start / Pause | 一時停止 |
| Resize Map S/M/L | ズーム3段 |
| Unit / Progress / Graph Window | ページが3つとも描いている |
| Overall Order (For new units) | `lm_set_order`（`DAT_004365e0`）|
| Overall Order (Override all) | `lm_order_all` |
| **Recall Leader** | **`lm_recall_leader`（`00421660` を王に掛ける）— 実装した** |
| Alliance | 下記のとおり「解消」しか実装のしようがない |
| Leader Position, Customize, Help | 未実装 |

**同盟について**: `at1e`（+0x1e、0x80 で同盟なし）に**国番号を書く場所が
実行ファイル中に1つも無い**。書き込みは `= 0x80`（解消）の2箇所だけで、
片方が `00411eb0`、"Break alliance" と描く UI ハンドラ。
つまり同盟は `at08`（首都）や `at0c`（王の実体番号）と同じで
**シナリオ／セーブが与えるもの**で、ゲーム中に結べるものではない。
メニューの Alliance は解消専用。

## 引き継ぎ (2026-09-02 夕方 時点)

**今日ここまでで変わったこと（大きい順）**

1. **4か国が伸びて・ぶつかって・滅びるまで通った。** 15面のうち12面が
   4000〜9600掃引で決着する。止めていたのは `0041e1d0` が `0041e670` を
   訊いていなかった／`00421d30` の後半（壁と鉱脈を掘る）が無かった／
   `0041ad90`（地面が開いたら通行可否を引き直す）が丸ごと無かった の3つ。
2. **略奪が自分も削るようになった** (`004209a0`)。村を焼くのが無料だったのが
   1国の一方的な塗りつぶしの原因だった。
3. **`00421ae0` の末尾はプレイヤーの国にも効く**。仕事の無いユニットは
   誰の国のものでも `00421ba0`→`00422040`→`00422110` に落ちる。命令名の
   1 が "Auto" なのはこれ。弱いユニットは建てた集落に自分を使い切るので
   **開始数百掃引は常備軍ゼロ**、集落が育ってから軍が出る。
4. **命令の出し方を原作どおりに直した** — マスを先にクリックして、その場に
   出るメニューから選ぶ。橋の架け方はこれ。詳細は上の 00423940 の節。
5. **文字が出るようになった**（東雲16ドット）。**メニューバーも canvas に
   Windows 95 の見た目で出る**。詳細は上の節。
6. 五つの作業命令に**射程1マス制限** (`0041ac10`) と対象を向く処理
   (`0041ac40`)、地形の**自動タイル貼り** (`0041acc0` と4つ)、
   国の色をゲームのパレットから、部隊のクリック／ドラッグ選択。

**動いているもの**: マップ・スプライト（3ズーム）・パレットの脈動・
シミュレーション一式・コンピュータの行動一式・王の生産と出撃・
国の滅亡と面の決着・音楽・状態の吹き出し・カーソル・面の名前と順・
絵・**文字**・**メニューバー**・**命令メニュー**・ネイティブと WASM 両方。

**試験** (`sh tools/build_tests.sh`、全部緑):

| | 何を見ているか |
|---|---|
| `state_test` | 構造体のオフセットと個々の挙動 |
| `menu_test` | 地形ごとに出せる命令の表とメニューの操作 |
| `font_test` | 全15面の文字列にグリフがあるか（両リリース） |
| `orders_test` | プレイヤーの手順で命令8種＋橋が通るか |
| `stress_test` | 全15面の耐久（40000掃引でも緑） |
| `growth_test` | 全15面で伸びる→戦う→滅びる、と**うろうろ率** |

`node tests/wasm_check.js` はページの手順そのまま（選ぶ→時計→狙う→命令）。

**次にやると効くもの、順に**:

1. **simtower_wasm の Win32 shim に載せ替える**（上の節。ユーザー合意済み）。
   `src/ui.c` はその繋ぎなので捨てられる。フォントは `src/font16.c` を
   shim の `win32_font.cpp` に足す
2. 原作の**ツールバー**（`Hide Tool Bar` = 60005 があるので窓として存在する）。
   まだどんな絵かは読んでいない
3. `Leader Position`(40080-40083) が何をするのか未解明。`Alliance` は
   **実行ファイル中に同盟を結ぶコードが無い**（解消だけ）ので実装不要
4. B_009 / B_105 は 64000掃引でも決着しない。戦ってはいる
5. B_005 / B_104 / B_103 の3面は**プレイヤーが橋を架ける面**。機械は架けない

**触るときの注意**:

* ビルドは必ず `tools/lowpri.sh` 経由（並列ビルドでマシンが固まる）
* 窓を開けるテストはしない。`tests/frame_dump.exe` が PPM を書く
  （`bar:1` でメニューバー、`menu:col,row` で命令メニューも描ける）、
  `tests/sprite_sheet.exe` がバンクを並べる、`tests/ui_dump.exe` が
  インターフェイスのシートを出す
* `ds7e.zip` / `ds7j.zip` はリポジトリに入っている（ファルコムが
  https://www.falcom.co.jp/monarch/ で今も無償配布しているもの）。
  **他のゲームのデータは入れない**
* シェル経由のパッチでバックスラッシュが潰れる。`\n` を含む書き換えは
  **ファイルに書いた python スクリプト**か Edit ツールで。ヒアドキュメントは
  だめ（`\\n` が `\n` になり、C の文字列に生の改行が入る）
* `lm_step` は**回数を引数に取る**。引数無しで呼ぶと0回進む

## UI: 数字フォントとステータス表示（実装済み）

`data1.bz` のシートは `worldLoadStage` が読み込んで `World.ui` に持つ（全ステージ
共通、1回だけ）。**透明は 0x70**、他は `+0x70` されているのでパレットは
data1.rgb の先頭48色を **0x80** に置く（`gfxUiPalette`）。

シート上のフォント位置（インクの入った列を数えて実測）:

| フォント | 位置 | 大きさ |
|---|---|---|
| 大・白 | (0, 0) | 8 x 16 |
| 大・赤 | (0, 16) | 8 x 16 |
| 小・白 | (0, 32) | 4 x 8 |
| 小・赤 | (0, 48) | 4 x 8 |

`renderNumber()` が右詰めで数字を描き、`renderStatus()` が画面上部に
**資金 / 税率・戦力**を勢力ごとに出す（敗北した勢力は赤のフォント）。

嵌りどころ: 描画面のパレット解決は**3つの帯**を見分ける必要がある —
地形 0x10..0x1f、スプライト 0x30..0x3f、**UI 0x80..0xaf**。ホスト側の
カラーテーブル生成でこれを分けないと UI が地形の色で出る。

## 戦力の集計 (0041b370 前半)

* セル: 中立の湧き点(地形5)の価値は**勢力4**へ、ユニットセル(8+f)の価値は
  その勢力の `+0x30` へ（10万で飽和）、同時に `+0x2c` にセル数を数える
* エンティティ: **リーダー(+0x0d bit5)を除いた**戦力を `+0x28` へ（同じく飽和）、
  `+0x24` に数を数える。リーダーの戦力は国力に入らない
* **戦力(+0x10) = +0x28 + +0x30**、最後にもう一度 10万で飽和

`0041b370` は面の開始とセーブ読込で呼ばれる（毎フレームではない）。
**最後の敗北判定だけは別**にしてある — あのフラグは誰もクリアしないので、
毎フレーム呼ぶと一度でも戦力0になった勢力が永久に敗北扱いになる。
`stateRecomputeTotals()`（集計、いつでも呼べる）と
`stateMarkDefeated()`（面の開始時だけ）に分けた。



`data1.bz` の 256x1024 シートは展開済みで `tests/ui_dump.exe` が正しい色で
好きな行範囲を書き出せる（パレットは data1.rgb の先頭48色が palette 0x80）。
判明している場所:

| シート上の位置 | 中身 | 描いている原典 |
|---|---|---|
| offset 0x1d000 = y 464、`index << 4` | 16x16 のアイコン群（吹き出しの中身） | 1738 付近 |
| offset 0x1e000 = y 512+ の別系統 | 同上の第2セット | 1786 付近 |
| offset 0x20000 = y 512、208x64 | **吹き出し（メッセージ枠）** | FUN_0040ffe0（4箇所から） |

つまり残っている作業は「各パーツのシート上の位置」ではなく、**ウィンドウの
レイアウトとテキスト描画**（情報ウィンドウ、コマンドウィンドウ、数字フォント）。
そこは数千バイトの配置コードなので、まだ手を付けていない。

現状は HTML 側のパネルが同じ情報（資金・税率・領土・ユニット数）を出している。

## 音楽 (src/midi.c)

原作は `.MID` を MCI に渡して Windows のシンセに任せている。ブラウザにはそれが
無いので、**MIDI の解析と発音を C で自前実装**した:

* 標準 MIDI の可変長数値、複数トラックの時間順マージ、テンポメタ(0x51)追従
* 1音1ボイス（最大24）、鋸波と三角波の混合、短いアタックと減衰、
  チャンネル10はノイズ（打楽器）、最後に tanh でソフトクリップ
* General MIDI の音色は再現していない。**音程と長さを合わせるところまで**

検証はウィンドウも音も出さずにできる: `tests/midi_test.exe` が WAV を書き、
半秒ごとの主要音高を数えると D4→G4→E5→C4→D5 と旋律として動くことが確認できる
（ノイズなら音高が定まらない）。`tests/wasm_check.js` は WASM 越しに
rms > 0.01 と範囲内であることを見る。

## ファイルは host 経由（zip も読める）

原作のデータはリポジトリに置かない方針なので、`src/host.c` が2つの供給元を
同じ形で見せる:

* `hostUseDirectory()` — 展開済みの `DS7E_WIN/`（ネイティブとハーネス）
* `hostUseZip()` — **プレイヤー自身の `ds7e.zip` をメモリのまま**（ブラウザ、
  ネイティブでも可）

`src/zip.c` は central directory の走査と **inflate をスクラッチ実装**（fixed /
dynamic huffman、stored も）。ブラウザに任せず C で書いたのは、ネイティブの
ハーネスがページと同じ zip を読めるようにするため。エントリ名は**末尾一致**で
引く（zip 内は `DS7E_WIN/MAP/B_000.MAP`、呼ぶ側は `MAP/B_000.MAP`）。

確認: `tests/zip_test.exe ds7e.zip` が実アーカイブ（102 エントリ）から
マップ・タイルバンク・UI・パレット・256色静止画を取り出してサイズを検証する。
`tests/sim_harness.exe ds7e.zip B_003.MAP` のように**zip を直接**渡せる。

## 試験はウィンドウを出さずに

`tests/sim_harness.exe <dataDir> <map> [sweeps] [build:col,row ...]` が
**ウィンドウを一切開かずに**シミュレーションを回して状態を表示する。Win32 の器は
ただのビューアで、ゲームの中身は world.c / state.c / sim.c にあるので、
コンソールから全部動かせる。画面を占有しないので、このマシンを人が使っている間も
安全に回せる。全15面の一括確認もこれでやる。

`tools/click_shot.ps1` と `tools/shot.ps1` は実際のウィンドウを撮るが、
フォーカスを奪うので**必要なときだけ**。

## ビルドは必ず低優先度で

このマシンは並列コンパイルでデスクトップごと固まる。**必ず `tools/lowpri.sh`
経由**でビルドする（`cmd start /LOW` で生成時に優先度クラスを設定するので、
コンパイラドライバが起こす子プロセスも全部継承する。後から探して renice する
より軽くて確実）。`tools/build.sh` と `tools/build_tests.sh`、
`tools/build_msvc.bat` はすでにそうなっている。

## 罠

* **`small` は使えない**。`rpcndr.h` が `#define small char` しているので、MSVC で
  構造体メンバに `small` と書くと C2628 で落ちる（mingw は通るので気づきにくい）。
  `bank8/bank16/bank32` に改名した。
* `offsetof` は `<stddef.h>`。

## 環境

* Ghidra 12.1.3 + JDK 21 を `C:\prog\ghidra` に。再実行:
  `JAVA_HOME=C:/prog/ghidra/jdk-21.0.12.1+1 /c/prog/ghidra/ghidra_12.1.3_PUBLIC/support/analyzeHeadless.bat <proj> lm -import <exe> -scriptPath tools/ghidra_scripts -postScript DecompileAll.java <outdir>`
  プロジェクトディレクトリは**事前に mkdir が要る**（無いと即 abort）
* 出力 decomp/（all.c 32,240行、functions/*.c、index.csv）は**コミット済み** —
  RESUME.md がアドレスと行番号で参照しているので、これが無いと追えない。
  詳細は `decomp/README.md`
* ビルドマシン 192.168.6.14 はこの時点で SSH 不通だったのでローカルで実行
